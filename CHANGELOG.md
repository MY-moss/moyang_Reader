# 更新日志

## [Unreleased]

后续功能切片继续在此累计。

## [0.10.8] - 2026-08-28

### Added

- 文件和文件夹右键支持只读属性查看；标签页右键支持关闭当前、其他、右侧和全部。
- Markdown WYSIWYG 与源文本编辑器右键支持查找选中文本。

## [0.10.7] - 2026-08-28

### Fixed

- 修复 Windows 桌面导出 PDF 时 Edge headless 子进程异步落盘导致文件尚未出现就被判定失败并清理的问题；现在确认有效 PDF 文件生成后再执行原子替换。

## [0.10.6] - 2026-08-28

### Added

- 文件树右键增加复制文件/复制文件夹、复制相对路径和展开/折叠文件夹；副本仍限制在已授权工作区内。
- Markdown/TXT 编辑器右键增加清除常见 Markdown 格式、任务列表和插入今天日期，源文本与所见即所得模式保持一致。

## [0.10.5] - 2026-08-28

### Added

- 文件树右键增加打开、新建、重命名、删除、资源管理器定位和复制完整路径；支持空工作区根目录菜单，重命名后同步更新打开标签、最近记录和当前文档路径。
- Markdown/TXT 编辑器右键增加撤销、重做、剪切、复制、粘贴和全选，并复用应用级编辑历史与现有格式/段落/插入动作。

### Fixed

- 文件/文件夹管理由 Tauri 端执行授权、路径穿越、目标重名和最终符号链接校验；删除当前条目会安全清理受影响标签并尝试打开相邻标签。
- 资源管理器定位使用无控制台窗口的 Windows 原生调用，避免额外 CMD 窗口干扰应用生命周期。

本版本只发布 Windows x64 安装包；签名、manifest 和镜像资产由 Release workflow 生成并在发布后核验。

## [0.10.4] - 2026-08-28

### Added

- #232（第二批）：标签页支持鼠标中键关闭和原生拖拽排序；阅读区支持 Ctrl+滚轮连续缩放、Ctrl+=/− 和 Ctrl+0 复位，设置提供 75%–150% 调节，并兼容旧版正文字号偏好。

- 工作区侧栏支持新建 Markdown 笔记和真实文件夹；根目录、文件夹及文件所在目录提供右键新建菜单，空文件夹可持续显示。
- Markdown WYSIWYG 与 TXT/Markdown 源码编辑器提供轻量右键菜单，支持格式、段落和插入动作。

### Fixed

- 修复工作区增量刷新无法保留新建空文件夹的问题，创建后目录可立即显示并在重载后恢复。

## [0.10.3] - 2026-08-27

### Added

- 增加首次使用“快速上手”教程，可从首次启动、空白页或设置重新打开；覆盖打开文档、添加多个阅读库、编辑、搜索、上下文面板、导出和更新。

### Fixed

- 设置保存改为统一快照并在写入后校验；Windows 桌面端增加应用配置文件的原子读写兜底，设置面板显示保存中、已保存、备用存储或失败状态。
- 设置保存状态改用主题自适应的高对比度文字，修复首次 CI a11y 检查发现的 serious `color-contrast` 问题。

## [0.10.2] - 2026-08-27

### UI

- #187：Windows 最小窗口宽度调整为 720px；响应式工具栏在操作真实溢出时提供横向滚动提示，避免紧凑窗口下隐藏操作没有反馈。

### Quality

- PR #269：补充 390px 紧凑视口的工具栏溢出、正文无横向溢出和顶部菜单互斥回归验证；Windows x64 Quality checks 全部通过。

### UI

- #187：Windows 最小窗口宽度调整为 720px；响应式工具栏在操作真实溢出时提供横向滚动提示，避免紧凑窗口下隐藏操作没有反馈。

## [0.10.1] - 2026-08-27

### Performance

- #104：完成大工作区搜索验收；未变化工作区复用索引和文件列表缓存，ASCII 子串查询复用 posting 候选，posting 达到预算时按文件 LRU 淘汰并对超限文件安全回退线性搜索。5000 篇每篇至少 2 KiB、混合中英文文档的暖查询 P95 本机为 38 ms，目标低于 100 ms。

### Fixed

- 撤回/重做后保留中央阅读区、Milkdown 和 CodeMirror 的滚动位置，避免 `Ctrl+Z` 打断长文阅读。

### Quality

- 增加超长文档回退、索引一致性、缓存失效和 5000 文档性能回归；Windows x64 Quality checks、Rust 测试、格式检查和静态检查通过。

## [0.9.5] - 2026-08-27

### Fixed

- #187（部分修复）：右侧目录改为在当前文档的中央滚动容器内定位标题，支持编码后的标题 ID，并在渐进渲染或编辑模式切换后有限重试；完整响应式断点问题仍待后续验收。
- 清理左侧文件树、搜索结果和导出失败列表的嵌套滚动层，减少纵向和横向滚动条造成的视觉干扰。

### Added

- 左右侧栏支持指针拖拽、键盘方向键、Home/End 调整宽度，双击恢复默认值，并在本机记住布局；新增右侧上下文面板快捷键 `Ctrl+Shift+R`。

## [0.9.4] - 2026-08-27

### Performance

- #183：标签页切换增加会话级文档缓存，按路径、文件大小和修改时间校验；最多 32 条、总估算内存 64 MiB，命中时避免重复读取和 Markdown/Word 准备流程，文件变更或保存后自动失效。

### Quality

- 完成前端单测、浏览器 smoke、Windows 桌面 smoke、Rust 测试、静态检查和发布前门禁；发布后补充 GitHub Release、镜像资产和旧版本更新实测结果。

## [0.9.3] - 2026-08-27

### Added

- Windows 桌面版当前文档支持选择保存位置并生成真实 PDF 文件；导出使用隐藏的 Edge 渲染进程，完成文件头校验后再原子写入，浏览器预览行为保持不变。

### Quality

- 完成已登记 Windows x64 v0.8.0 安装实例到 v0.9.2 的更新下载、签名校验、替换、自动重启和版本号实机回归。
- 发布前继续验证 Windows x64 安装包、`.sig`、`latest.json`、Cloudflare 镜像和旧版本自动更新链路。

## [0.9.2] - 2026-08-27

### Fixed

- #174：应用根节点新增 React 渲染错误边界；渲染期异常显示可恢复页面，不再直接白屏，错误详情只保留在本地。
- #188：统一阅读模式、所见即所得编辑器和 HTML 导出的 h4/h5/h6 层级，以及列表缩进、嵌套间距和列表项节奏。
- #231：顶栏“更多 / 设置 / 导出”菜单支持外点和 Escape 关闭，且不干扰编辑器内部的 Escape 行为。

### Quality

- v0.9.2 稳定批次继续沿用 Windows x64 桌面 smoke、浏览器 smoke、前端覆盖率、依赖审计、发布预检和 Rust 质量门禁；安装包、签名、manifest 与镜像在发布完成后单独核验。

## [0.9.1] - 2026-08-27

### Release

- 发布链路：Cloudflare 镜像改为 Release 资产静态上传，缺少部署凭据时正式同步直接失败，并在传播延迟或临时 HTTP 错误后重试校验版本目录、安装包和签名文件。

### Added

- v0.9.1 编辑稳定化切片：源码编辑器和 Markdown 所见即所得编辑器共用有界编辑历史，支持 `Ctrl+Z`、`Ctrl+Y` / `Ctrl+Shift+Z`、编辑工具栏和命令面板；打开或切换文档时重置历史，保存后仍可撤销并重新标记为未保存。

### Fixed

- #104：全文搜索索引达到全局 posting 预算时，超限文件单独回退线性扫描，不再让整个工作区索引失效；已有索引文件继续使用增量候选筛选。
- 草稿恢复中心的逐条丢弃改为可取消的确认对话框；当前文档的草稿提示增加“稍后处理”，只隐藏提示而不会删除本地草稿。

## [0.9.0] - 2026-08-26

### Performance

- #177：WYSIWYG 与渲染模式的 Markdown 更新统一采用 180ms 可取消防抖，快速输入不再为每个中间状态立即运行完整 Markdown/KaTeX 渲染管线；保留请求序号保护，旧渲染结果不会覆盖新草稿。

### Added

- 工作区：明确区分“添加阅读库”和“切换阅读库”，最多同时挂载 5 个本地文件夹；每个阅读库独立保留文件树、筛选、标签页和当前文档，达到上限时不再静默挤掉旧库。
- 编辑：Markdown 打开后在顶部提供直接的“编辑 / 阅读”入口，所见即所得模式不再必须先打开“更多”菜单；源码模式仍保留为高级保真路径。

### Quality

- #88：真实 Tauri Windows desktop smoke 新增工作区双链候选、WYSIWYG `[[` 接受与源码 `/ul` 块级命令回归；测试专用编辑器桥只在 `wdio` 构建注入，普通安装包不包含测试入口。
- UX：收敛“添加整个文件夹”入口；正常显示侧栏时只在空白主区或已打开阅读库的侧栏提供对应动作，侧栏收起后再由顶部按钮接管；已有阅读库的空白主区不再重复引导添加文件夹。
- UX：工作区根目录文件不再渲染空的路径副行；文件树、快速打开、最近记录和批量导出仍保留各自职责，不恢复无选择批量打开。
- Windows：Tauri Debug/Release 入口统一使用 GUI 子系统；开发服务器通过隐藏窗口的辅助进程启动，避免额外 CMD 窗口与应用生命周期互相误导。

### Fixed

- #185：切换文档、关闭标签、打开文件、创建笔记或切换阅读库前会同步冲刷最新本地草稿；桌面文件成功保存后确认文案会明确提示可在“草稿”中心恢复，草稿保存失败则阻止切换。
- #186：统一命令面板、快速打开、草稿恢复、关系图、打印预览和确认弹层的初始焦点、Escape 关闭、Tab/Shift+Tab 焦点循环及关闭后焦点归还，避免键盘操作逃逸到背景页面。
- #180：切换或关闭文档时改用每个文档最后一次已知的阅读位置保存，不再从已经切换到新文档的滚动容器读取旧文档位置；恢复动作会在真正进入阅读模式并完成布局后有限重试，无效或负的滚动值会安全归一化。

## [0.8.3] - 2026-08-26

### Fixed

- #170：系统跟随暗色外观且未显式选择主题时，文档内搜索的普通命中和当前命中现在使用暗色高亮，不再沿用亮色背景造成对比度和可读性问题。

## [0.8.2] - 2026-08-26

### Added

- v0.9.0 首个功能切片：三栏阅读工作区、右侧上下文面板状态持久化、Markdown Milkdown WYSIWYG 按需加载、复杂语法源码安全回退、命令面板和 Markdown 链接快捷键。
- v0.9.1 编辑稳定化切片：外部无冲突修改会同步到 WYSIWYG 编辑器；存在未保存编辑时先保留草稿并确认后再重载，应用自身保存事件不会重复触发外部修改提示。
- 源码模式和 WYSIWYG 编辑器均支持 `[[` 双链补全：候选来自当前工作区 Markdown 文档，支持键盘上下选择、Enter/Tab 确认、Escape 关闭和鼠标点击；补全浮层显示文档名与相对路径。
- 新增 Milkdown 支持语法 round-trip 样例 e2e：标题、行内样式、双链、嵌套/任务列表、引用、代码块、表格、图片和分隔线经 WYSIWYG 编辑器序列化后内容无损。
- 同步新增 `CONTEXT.md`、需求、路线、UI 交互、AI 交接和两份 ADR，统一后续 AI 接手与发布流程。
- 路线文档新增 v0.9.2 编辑器交互深化切片和 v0.10.2 UI 视觉与微交互深化批次（设计令牌统一、保存状态反馈、`/` 块级触发器、a11y 深化、性能感）；AI 交接文档新增可粘贴的快速启动模板；round-trip 风险点已记录为 #156（间歇性标题损坏观察）、#157（序列化样式规范化清单与方案）、#158（e2e 依赖 CodeMirror 内部路径）。
- 新增真实 Tauri 桌面 smoke：启动参数打开 Markdown、默认所见即所得编辑、源码模式编辑和 Rust 保存写回均在 Debug 桌面进程中验证；测试权限与 WebdriverIO 桥接隔离在测试配置，不进入普通构建或安装包。
- 真实桌面 smoke 扩展到工作区外部修改自动刷新；路径比较补齐 Windows `\\?\\`/UNC 扩展路径，避免 watcher 事件因路径格式不同而漏掉当前文档。
- 真实桌面 smoke 增加未保存冲突场景：外部修改只显示提示并保留本地编辑内容，不静默覆盖草稿。
- 固化版本发布规则：用户功能 minor 版本和重要 Bug patch 版本必须同步 Release、安装包、签名、`latest.json` 与镜像；纯文档/测试改动可不发布。
- CI/Release 依赖审计明确只阻断会随应用发布的生产依赖；WDIO、Playwright 等仅用于测试的工具链继续由 Dependabot 和本地/CI 测试单独跟踪，避免测试工具的间接漏洞阻断非发布功能 PR。
- 工作区文件树不再先截断 80 项再要求用户点击“显示全部”；当前筛选结果一次构建完整目录树，避免深层文件夹和文件计数被截断造成误导。大工作区虚拟化仍按 #169 规划。
- Windows 桌面端将未保存退出提示改为应用内可访问对话框，支持取消后继续编辑、Escape 取消和显式确认退出；避免原生确认框阻塞 WebView 自动化与键盘焦点。
- 更新下载进行中允许隐藏提示，下载不会被误关闭；顶部“下载中…”按钮可重新打开进度。启动更新检查偏好只在下一次启动时读取，运行中切换设置不会意外触发检查（#192）。

### Removed

- 移除“打开列表”入口：它会在没有逐项选择的情况下把最多 40 个筛选结果批量塞进标签页，和文件树、Ctrl+P 快速打开及系统多选打开重复；保留更可控的单击打开、快速打开和批量导出。
- 移除右侧只负责再次触发“打开关系图”的空占位页签；关系图入口统一放在“关联”视图中。未打开阅读库或当前筛选没有可导出文档时，也不再显示无可用选项的“批量导出”菜单。

### Fixed

- 修复外部修改提示被“稍后处理”后失去保护的问题：文档会持续保留外部修改标记，普通保存会被拦截，用户必须选择重新载入、覆盖保存或另存为；覆盖保存使用应用内二次确认，并在写入前复核磁盘内容，避免静默覆盖第三方修改（#178）。自身写入 watcher 事件改为写入中忽略、写入完成后再进入短暂抑制窗口。
- 修正右侧文档状态卡片在外部修改后仍显示“已保存”的误导文案，改为明确显示“外部已修改”。
- E2E 的 CodeMirror 文本读取在内部 view state 路径失效时不再静默回退到可视区 DOM；现在会明确提示 `CodeMirror internal view state path changed — update readEditorText`，避免编辑器依赖升级后产生“末段丢失”的误报（#158）。
- 修复 WYSIWYG 编辑器自 v0.9.0 起从未真正挂载的严重缺陷：`@milkdown/preset-gfm` 7.22.x 的 `gfm` 预设不再内含 commonmark 基础节点，ProseMirror schema 因缺少 `doc` 顶层节点创建失败，编辑区静默空白。现显式注册 commonmark + gfm 预设，新增编辑器挂载回归单测，round-trip e2e 改为等待真实 contenteditable 并在编辑后校验序列化结果；编辑面挂载失败时会显示可见错误提示而不是空白。
- 修复 round-trip e2e 误报“末段丢失”的读取缺陷：CodeMirror 只渲染可视区内的 `.cm-line`，直接读 DOM 会把长文档尾部截断。现通过 CM 内部 view state 读取完整文档文本，并完善序列化样式归一化（`*`/`-` 列表符、`***`/`---` 分隔线、`\[\[` 双链转义、表格分隔行宽度）。
- 为 WYSIWYG 编辑面补充 ProseMirror 要求的 `white-space: pre-wrap`，消除控制台告警并避免尾随空格/软换行在渲染层被折叠。

### Planned

- v0.9.0：未知语法回退提示和真实 Tauri 桌面路径继续完善。
- v0.9.1：编辑器 round-trip 保真、桌面 E2E、a11y 和基础 i18n 稳定化。

## [0.8.0] - 2026-08-25

### Added

- 大文档按块渐进式渲染，减少打开长文时的首屏等待和滚动卡顿。
- 本地草稿恢复中心、持久化搜索索引、批量导出失败清单和更完整的打印页码支持。
- 内置文档适配器注册表，统一 Markdown、文本、DOCX、PDF 和图片格式能力，后续格式可按适配器接入。
- 设置/工作区迁移备份 JSON，只保存本机偏好、阅读库路径、会话和标签页，不包含文档正文或私钥。
- 更新失败恢复提示、发布工作流自检、核心工具栏/设置区中英切换和 macOS/Linux 构建前置 CI。

### Quality

- 增加 axe-core 无障碍冒烟门禁，覆盖空白页、阅读界面、快速打开和设置面板。
- v0.8 仍以 Windows 稳定发布为主，跨平台先验证构建、测试和路径行为；外部任意脚本插件与云端同步暂不开放。

## [0.6.0] - 2026-08-25

### Added

- 批量 HTML、Word 和打印/PDF 导出过程中可随时取消；取消会在当前文档处理完成后停止，并避免进入最终写入或打印预览。
- 多个阅读库可以同时挂载并快速切换，每个阅读库分别记住搜索、标签、类型筛选、打开标签页和当前文档。
- 应用重启后恢复各阅读库的标签页与最后文档；只保存本地路径和界面状态，不保存文档正文。
- 源文本编辑器升级为 CodeMirror，增加 Markdown 语法高亮、搜索替换和更稳定的大文本编辑体验。
- 支持把剪贴板图片直接粘贴为 Markdown 图片资源，并继续保留未保存草稿的恢复能力。
- 工作区搜索增加有界候选索引与缓存，超大目录搜索不再每次重复扫描全部文件。

### Fixed

- 源文本编辑后切回阅读视图会使用当前草稿重新渲染，不再显示磁盘中尚未保存的旧内容。
- 浏览器选择或拖入未知扩展名文件时会明确跳过并提示，不再将二进制文件误当作 Markdown 渲染。
- Word 导出中的有序列表会按实际条目顺序输出 1、2、3 编号，嵌套列表也会从每一层重新计数。
- 浏览器中打开同名文件时会使用唯一的内部文档标识，标签页和文档内容不再互相覆盖。
- Word 导出会保留安全的 HTTP、HTTPS 和 mailto 外部超链接，并把 wiki 链接归一化为相对 `.md` 目标。
- 关系图在不同窗口比例下的连线与节点位置保持一致。

### Performance

- PDF 批量预览按批次处理，减少大量文档一次性占用内存。
- Markdown 渲染、链接索引和 HTML 序列化处理器复用缓存，降低重复打开与批量导出的开销。
- DOCX 导出改为流式读取并支持取消，发布镜像准备也改为更快的批量流程。

## [0.5.4] - 2026-08-24

### Added

- 长文档阅读区增加右侧轻量阅读轨道，显示阅读百分比、当前章节和章节数量，并提供顶部/末尾定位。
- 阅读轨道在窄窗口和专注阅读模式下自动收起，避免挤压正文；左侧继续专注于工作区、目录和关联内容。
- 左侧文档目录会随正文滚动同步高亮当前章节，长目录自动跟随当前条目，点击目录后右侧进度和当前章节也会同步更新。
- 单文档 HTML、打印 / PDF 导出在有多个标题时增加可点击的文档目录，保留原文标题层级并避免目录在打印时被拆开。
- 批量 HTML、Word 和打印/PDF 导出增加当前文件、进度和总数提示，长时间处理时可以确认任务仍在继续。
- 批量导出失败项现在可以在侧栏展开查看完整相对路径和具体原因，切换阅读库或筛选时会自动清理旧结果。
- 批量打印/PDF 导出现在先进入应用内版式预览，确认目录、分页和纸张设置后再调用系统打印。
- 打印版式预览底部增加纸张设置与预计页数状态，窗口尺寸或图片布局变化后会自动重新估算，并明确以系统打印分页为准。
- DOCX 导出增加统一页眉、页脚和“第 X / Y 页”字段，打开后由 Word 自动更新总页数，适合正式转发和打印。
- 已打开阅读库时，左侧路径旁新增“切换”入口，可直接切换最近阅读库，不必先关闭当前阅读库或重新选择文件夹。
- 文件/文件夹打开、批量打开、最近阅读库切换和文档内链接现在统一保护未保存修改，取消确认即可留在当前文档。
- 工作区全文搜索会缓存已排序的文件清单，连续搜索不再重复递归枚举目录；文件监听刷新后会自动失效并重建缓存。

### Fixed

- 修复长文档把应用主网格整体撑高的问题；现在滚动位置记忆、阅读进度和专注阅读都作用于正确的阅读容器。

## [0.5.3] - 2026-08-24

### Added

- 启动参数和单实例请求现在支持传入整个文件夹，并会自动载入为阅读库；支持文档文件的原有打开方式保持不变。
- 桌面版支持将多个文档或整个文件夹直接拖入窗口，自动按类型打开文档或载入阅读库。
- 扩展安装包文件关联：文本、Word、PDF 和常见图片可以直接双击交给 Moyang Reader 打开。
- 优化打印 / PDF 版式：无一级标题的文档会补充文件名，代码块、表格和段落分页更适合转发阅读。
- 工作区批量导出增加单文件 Word（DOCX）选项，文档之间自动分页；原有单文件 HTML 导出保持不变。
- 增加当前文档富文本复制，支持标题、列表、表格和代码等常见内容，并提供纯文本剪贴板回退。
- 桌面和浏览器“打开”入口都支持多选文档并依次加入标签页；取消选择时不再额外弹出第二个文件选择器。
- 阅读库批量导出增加“批量打印 / PDF”入口，会把当前筛选的可导出文档整理后交给系统打印对话框。
- 增加专注阅读模式：隐藏目录、标签和状态栏，支持按钮、Esc 和 Ctrl+Shift+Enter 切换。
- 设置中增加正文字号和正文宽度调节，并在本机记住阅读排版偏好。
- 阅读库增加“打开列表”，可将当前筛选文档依次加入标签页；单次最多打开 40 个并显示跳过提示。
- 阅读库增加按文档类型筛选，可联动搜索结果、批量打开和 HTML / Word / 打印导出。
- 工作区侧栏显示当前筛选结果数量，并提供一键清除标签/类型筛选，避免打开和导出范围不明确。
- 批量导出会列出跳过文件的相对路径和原因；文件较多时保留前 3 项并显示总数。
- 设置中增加导出纸张、方向和页边距，并同步应用到当前文档、批量 HTML / PDF 和 Word 导出；阅读区增加轻量纸面层次与文档信息提示。
- 导出菜单增加当前文档打印版式预览，支持 Esc / 点击关闭、查看纸张设置后再打印；原有主按钮的一键打印路径保持不变。
- 增加侧栏收起/恢复按钮和 Ctrl/Cmd+Shift+B 快捷键，收起状态会在本机记住，方便长文阅读时展开主阅读区。
- 长文档阅读区改为应用内滚动，新增右侧轻量阅读轨道，显示阅读百分比、当前章节并提供顶部/末尾定位；窄窗口自动隐藏阅读轨道。
- 桌面端会话恢复会记住有效的本地标签页，并在重启后打开上次活动文档；失效路径和浏览器临时文件不会恢复。
- 为外部路径消息增加文档/工作区类型标记，并补充 Rust 分类与前端桥接格式测试。

## [0.5.2] - 2026-08-24

### Added

- 增加快速打开面板：按文件名或路径即时筛选工作区、最近文件和已打开标签，支持键盘快速选择。
- 增加直接添加整个文件夹的入口，可从空白页、顶部工具栏和 Ctrl/Cmd+Shift+O 快捷键进入。
- 重启应用后自动恢复最近阅读库、最后打开的文档和本地文档的滚动位置。
- 记住的本地路径会在重新打开时重新校验并授权，避免应用重启后因会话授权清空而无法继续阅读。

## [0.5.1] - 2026-08-23

### Added

- 添加整个文件夹作为阅读库，并支持后台索引、目录浏览、搜索、标签和关系图。
- 空白页和顶部工具栏增加添加整个文件夹入口，并支持 Ctrl/Cmd+Shift+O 快捷键。
- 增加快速打开面板：Ctrl+P 按文件名/路径即时筛选工作区、最近文件和已打开标签，支持键盘上下选择与 Enter 打开。
- 支持 DOCX 阅读和真正的 DOCX 导出；HTML 可通过系统打印保存为 PDF。
- 增加签名更新包检查、下载、安装和自动重启。
- 增加最近阅读库、工作区大目录展开、Markdown 标题/链接/标签解析和导出资源上限。
- 修复最近文件和阅读库跨重启后因会话授权清空而无法打开的问题；现在会重新校验并授权已记住的本地路径，同时恢复最后阅读文档。
- 记住本地文档的滚动位置，重新打开时可从上次阅读处继续。

### Fixed

- 修复长文档把应用主网格整体撑高的问题；现在滚动位置记忆、阅读进度和专注阅读都作用于正确的阅读容器。

- 修复 TOC 锚点、重复标题、UTF-16 不完整字节、含括号链接和目录标题识别问题。
- 修复二进制文件通过 JSON 字节数组传输、编辑模式连续按键重复渲染和浏览器预览 URL 不回收问题。
- 原生文件/文件夹/保存选择器改由 Rust 侧登记授权路径，文本和二进制写入只允许落在用户明确选择的范围内。
- 保存使用带失败清理的临时文件替换；Windows 使用原子替换标志，避免先删除原文件导致内容短暂丢失。
- 文本保存成功后清理临时 `.moyang.bak`，避免每次编辑在工作区留下陈旧备份；失败时仍保留备份。
- 外部 http(s)/mailto/tel 链接改为交给系统浏览器或系统关联程序打开，启动参数传入的多个文件会依次加入标签页。
- 增加 Rust/Tauri 主窗口导航兜底：应用来源和开发服务器正常放行，外部网页、文件和不支持协议不能替换阅读器窗口。
- 桌面窗口关闭和浏览器预览离开页面时，会在存在未保存修改时先请求确认。
- 文本解码增加控制字符、NUL 和替换字符启发式检查，疑似二进制内容不会再静默进入可编辑状态。
- 读取和写入授权范围拆分：单独打开文档时可读取同目录相对附件，但不会因此获得同目录其他文件的写入权限；工作区文件夹仍按用户选择授予读写权限。
- PDF、图片和相对附件统一通过授权后的二进制读取生成 Blob URL，移除全局 `asset://**` 作用域；二进制 IPC 改为原始响应，避免 JSON 字节数组传输。
- 工作区文件监听改由 Rust 原生 watcher 按已授权工作区建立，移除前端 fs 插件和全局 `fs:scope ["**"]` 权限。
- 工作区变更继续按 280ms 合并事件，并只刷新变更文件/目录的文件列表和 Markdown 索引，避免每次保存整库重扫。
- 全文搜索缓存已解码文本，并按文件大小/修改时间和监听变更作用域失效，连续输入不再重复读取未变化文件。
- 反向链接改为按工作区快照复用目标索引，文件树改为按路径 Map 复用目录节点，减少大工作区切换和构建时的重复查找。
- 更新文档移除本机私钥路径、用户名和无密码状态，并在发布检查中阻止这类内容重新进入公开文档。
- 发布工作流的手动触发改为强制输入版本，并校验它与项目三处版本号一致，避免误生成分支名 Release。
- 建立 ESLint/Prettier/Rust clippy 门禁和 Rust 构建缓存，并完成现有前端源码的格式化基线。
- CI 和 Release 增加 RustSec cargo-audit 依赖漏洞门禁，并保留 Dependabot 的 npm、Cargo 和 GitHub Actions 更新配置。

### Changed

- Markdown 和 DOCX/HTML 适配器共用统计、文本和 TOC 工具。
- 将 `jszip` 放入运行时依赖，并升级前端构建/测试工具链以通过依赖审计。
- 增加本地隐私设置：远程图片和启动更新检查默认关闭，可分别开启；手动更新检查始终保留。

## [0.5.0] - 2026-08-23

- 初始公开版本，提供快速本地文档阅读、文件关联、主题和基础导出能力。
