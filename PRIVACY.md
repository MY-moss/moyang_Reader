# 隐私说明

Moyang Reader 是本地优先的阅读器，不要求登录，也不会把本地 Markdown、Word、PDF 或图片内容上传到 Moyang Reader 服务。

## 会访问网络的情况

- 启动后会从 GitHub Releases 检查更新；更新包必须通过内置公钥验证后才会安装。
- 如果文档包含 `http://` 或 `https://` 的远程图片、嵌入资源或链接，应用所在 WebView 可能按文档内容访问对应站点。
- 应用不会因为打开普通本地文档而主动上传文档内容。

## 本地保存的数据

- 最近打开的文件和阅读库路径保存在应用本地存储中。
- 编辑保存时会短暂创建备份文件和临时文件，以减少写入中断造成的丢失风险。
- 更新私钥不属于应用数据，不能放进仓库、安装包或公开文档；发布流程只使用本机安全文件和 GitHub Actions Secret。

## 用户控制

用户可以关闭更新检查、移除最近记录或删除本地备份文件。后续版本会继续完善远程资源开关、更新检查偏好和更细的文件访问作用域。
