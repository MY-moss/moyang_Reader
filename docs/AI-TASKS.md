# Moyang Reader — AI 任务队列

> 后续 AI 默认从上到下选择第一个 `TODO` 且没有开放 PR 的任务。一个任务一个分支、一个 PR。完成后改为 `DONE` 并附 PR 号；遇到新问题在末尾追加候选，不要重建复杂状态机。

## 当前优先级

### A01 — 完成 TS ↔ Tauri 命令契约集中化

**状态：IN_PROGRESS — PR #449**

- 目标：完成 #194 当前切片，把前端散落的 Tauri 命令名、参数和返回类型集中维护。
- 用户价值：减少前后端字符串漂移造成的静默错误。
- 非目标：不改变 Rust 命令行为；不做运行时响应校验。
- 主要文件：`src/app/bridge.ts`、`src/app/ipc-contract.*`。
- 验收：命令名不再散落手写；现有桥接行为不变；相关测试、lint、build 通过。
- 注意：旧 `docs/ai/state.json` / `docs/NEXT.md` 变更在轻量工作流合并后应从 PR 中移除或重做，产品代码本身继续保留。
- 回滚：恢复旧 bridge 常量，不涉及用户数据。

### A02 — 首批只读 IPC 运行时响应校验

**状态：TODO**

- 关联：#194
- 目标：为 3–5 个高频只读命令增加轻量运行时 shape 校验和统一错误。
- 用户价值：Rust 返回结构意外变化时更早发现，而不是在 UI 深处崩溃。
- 非目标：不一次迁移全部 IPC；不改写入命令。
- 主要文件：`src/app/bridge.ts`、`src/app/ipc-contract.ts`、相关 Rust 命令与 desktop E2E。
- 验收：旧有效返回继续通过；无效返回产生稳定错误；真实桌面只读路径通过。
- 推荐验证：bridge 单测 + build + desktop smoke。
- 回滚：移除运行时校验即可。

### A03 — 提取设置与偏好控制器

**状态：TODO**

- 关联：#16
- 目标：从 `App.tsx` 提取设置读取、写入、默认值与损坏恢复逻辑。
- 用户价值：降低 App 状态耦合，让后续 UI 修改更安全。
- 非目标：不重做设置 UI；不改设置 key 或持久化格式。
- 验收：设置正常读取/写入、损坏配置、写入失败均有测试；App 不再直接维护大部分设置细节。
- 推荐验证：settings 单测 + 相关 E2E + lint/build。
- 回滚：控制器逻辑可直接内联回 App。

### A04 — 提取文档会话生命周期

**状态：TODO**

- 关联：#16
- 目标：拆出打开、草稿、保存、外部修改冲突、关闭与恢复的会话控制器。
- 用户价值：最重要的文件安全路径不再散布在超大 App 组件里。
- 非目标：不改变保存格式；不增加自动覆盖策略。
- 验收：保存、草稿、切换、关闭、异常恢复、外部修改全部保持现有行为；新增针对性测试。
- 推荐验证：相关单测 + 浏览器 E2E + desktop smoke。
- 回滚：按控制器单独回退，无数据迁移。

### A05 — Rust commands.rs 第一阶段拆分

**状态：TODO**

- 关联：#16 / #194
- 目标：先把纯只读/文档解析命令迁到领域模块，主入口只保留注册与路由的一部分。
- 用户价值：降低 Rust 单文件维护成本，方便 AI 精确修改。
- 非目标：不一次拆完整个 commands.rs；不改 IPC 名称或授权逻辑。
- 验收：迁移命令行为和名称完全一致；Rust tests/clippy + desktop smoke 通过。
- 回滚：把该领域模块重新合回原文件。

### A06 — Rust commands.rs 第二阶段：工作区与索引

**状态：TODO**

- 目标：在 A05 稳定后拆工作区扫描、索引、搜索相关命令。
- 用户价值：将性能热点和文件访问职责分开维护。
- 非目标：不重做索引算法。
- 验收：搜索/扫描结果与排序不回归；大工作区相关测试通过。
- 依赖：A05。

### A07 — 统一快速打开 / 文内查找 / 阅读库搜索

**状态：TODO**

- 目标：明确三种搜索的名称、入口、快捷键和错误反馈。
- 用户价值：降低顶部栏认知负担，用户知道自己到底在搜什么。
- 非目标：不改核心搜索算法。
- 验收：三类搜索入口清楚；错误可关闭、重试并定位对象；键盘与读屏流程通过。
- 推荐验证：相关单测 + Playwright + a11y。

### A08 — CSS / 桌面视觉收口

**状态：TODO**

- 关联：#171
- 目标：按语义令牌和主要区域拆分样式，减少暗色/高对比重复规则。
- 用户价值：主题和控件修改不再到处漏改。
- 非目标：不重新设计品牌视觉；不引入 CSS-in-JS。
- 验收：浅色、深色、高对比、720/1240px 等关键尺寸截图/测试通过；硬编码色值继续下降。
- 推荐验证：style token 检查 + E2E/a11y。

---

## v0.12 — 稳定性与性能

### B01 — 可重复的大工作区性能基准

**状态：TODO**

- 目标：把 5k / 20k 文件工作区的扫描、冷搜索、暖搜索变成可重复 benchmark。
- 产物：固定语料生成器 + JSON 报告；CI 不设脆弱的毫秒硬门槛。
- 用户价值：后续性能优化有证据，不靠感觉。

### B02 — 大文件阅读与编辑降级策略

**状态：TODO**

- 目标：分别测量 1MB / 10MB Markdown 的首次可读、编辑切换、搜索、保存内存和耗时。
- 验收：形成明确阈值；超过阈值时关闭昂贵功能或提示只读/源码模式，而不是卡死。
- 非目标：不先假设优化方案，先测量。

### B03 — Tauri 权限库存与负向安全测试

**状态：TODO**

- 目标：列出 `opener` / `process` / `updater` 的真实调用点并补禁止协议/未授权路径测试。
- 用户价值：1.0 前确认最小权限，但不为“看起来更安全”盲目改 capability。
- 验收：`javascript:` / `file:` / 未知协议拒绝路径有测试；正常 http/https/mailto/tel 和更新流程不回归。

### B04 — 修复/关闭第十二轮审计遗留缺陷

**状态：TODO**

- 关联：跟踪 #373
- 做法：重新核对 #357–#366 当前 main，已修复的关闭，仍存在的按 0.5–3 天切片逐个处理。
- 禁止：不要根据旧行号直接改代码；必须先在当前 main 复现。

---

## v0.13 — 阅读工作台 → 轻量知识库

### C01 — 文档跳转历史 / 返回上一文档

**状态：TODO**

- 关联：#367
- 目标：双链、搜索、快速打开跳转后可返回上一文档/位置。
- 原则：只保存本地导航栈，不污染 Markdown。

### C02 — 阅读批注第一阶段：全文书签

**状态：TODO**

- 关联：#368
- 目标：在现有章节书签之外支持文档级/文本定位书签，先只做稳定定位和管理。
- 原则：旁路本地元数据，不改用户原文。

### C03 — Inbox 快速记录

**状态：TODO**

- 目标：用户指定阅读库中的一个目录作为 Inbox，一键创建普通 Markdown。
- 非目标：不做云同步、任务系统或数据库。
- 验收：其他 Markdown 编辑器可以直接读取；文件名冲突和写入失败安全处理。

### C04 — Daily Note

**状态：TODO**

- 目标：在用户指定目录按本地日期创建/打开当天 Markdown。
- 依赖：C03 的安全创建文件能力。
- 非目标：只提供最小模板，不做复杂脚本模板引擎。

### C05 — Properties 视图

**状态：TODO**

- 目标：把 YAML frontmatter 做成可读、可筛选、可编辑的轻量属性面板，Markdown 仍是真源。
- 原则：优先兼容常见标量/数组；不引入私有数据库作为内容真源。

### C06 — 属性 / 标签表格视图

**状态：TODO**

- 目标：基于索引生成类似数据库表格的只读/轻编辑视图，数据仍来自 Markdown frontmatter。
- 依赖：C05。
- 非目标：不复制完整 Notion/Bases 功能。

---

## v0.14 — AI 与扩展接缝

### D01 — DocumentAdapter 稳定接口
**状态：TODO** — 明确各格式的读取、预览、导出能力，不开放第三方脚本插件。

### D02 — IndexProvider 稳定接口
**状态：TODO** — 将工作区索引/搜索与 UI 解耦，为未来替换实现保留边界。

### D03 — CommandContribution 内部命令接口
**状态：TODO** — 统一命令面板、快捷键、顶部操作的内部注册方式。

### D04 — AiProvider + ConsentScope 原型
**状态：TODO** — 先做接口和本地 mock；发送用户内容前必须可见范围、provider、用途，写回必须有 diff。

---

## v0.15 / 1.0 — 冻结与发布

### E01 — 设置 / IPC / 快捷键兼容矩阵
**状态：TODO** — 冻结公共行为并补旧配置、损坏配置和旧版本兼容测试。

### E02 — 文件异常恢复矩阵
**状态：TODO** — 磁盘满、只读、外部删除、外部修改、异常退出、临时文件残留。

### E03 — Windows 安装/升级实机闭环
**状态：BLOCKED_EXTERNAL** — 关联 #241，需要真实 Windows x64 旧安装环境。

### E04 — Windows Authenticode
**状态：BLOCKED_EXTERNAL** — 关联 #51；有证书时接入，没有时明确披露和哈希核验，不把 updater `.sig` 当代码签名。

### E05 — 私密漏洞报告入口
**状态：BLOCKED_EXTERNAL** — 关联 #227，需要维护者开启 GitHub Private Vulnerability Reporting。

---

## 维护规则

- 队列只保留大约 10–25 个近期任务；完成项超过 10 个后移到 `docs/handoff/` 或对应 Issue/PR，不无限增长。
- 新任务优先复用现有 Issue；只有确认不存在重复问题时再新建 Issue。
- 不需要任务 digest、审批 JSON、动态 state 文件或生成 NEXT。
- 用户临时改变优先级时，直接移动 Markdown 条目即可，不需要迁移状态机。
