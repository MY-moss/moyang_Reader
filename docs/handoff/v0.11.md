# v0.11 交接记录

本文件按合并顺序记录 v0.11 已完成切片，并保留当前切片的交接记录。当前任务和下一步不写在这里，统一见 [`../NEXT.md`](../NEXT.md)。

截至 2026-09-02，v0.10.14 已作为独立 Windows x64 patch 版本发布；已合入的 v0.11 切片尚未单独发布，当前交接中的切片状态以 [`../NEXT.md`](../NEXT.md) 为准；下一次任务必须重新检查其中的 Ready backlog。

## #191 快速打开高亮跟随与读屏语义（第 1 个子切片，已合并，2026-09-02）

- 目标与用户价值：让快速打开结果在方向键或鼠标高亮变化后自动滚入可视区，并用稳定的活动后代语义让读屏用户知道当前选择；长结果列表中键盘用户不会丢失当前高亮，Enter 继续打开当前文档。
- 非目标：不实现标签栏、文件树或目录的 roving tabindex；不调整读屏播报/aria-live、专注模式、命令面板 Esc 互斥或其他 #191 子切片；不改变筛选、鼠标打开、文档内容、HTML、脚本、插件或发布链路。
- 基线与分支：基于已合并 `main@42337e840f2266f31715bee914630fc9b42cde1d` 创建项目内独立工作树；分支 `codex/quick-open-a11y-2026-09-02`；Issue [#191](https://github.com/MY-moss/moyang_Reader/issues/191) 启动前未发现重复产品 PR；PR [#424](https://github.com/MY-moss/moyang_Reader/pull/424) 已 squash 合并为 `main@a650f934429f8f19511dd6c72ef5b17541c694ff`，Quality checks run `33634427700` 全绿。
- 实现边界：搜索框通过 `aria-controls`/`aria-activedescendant` 关联结果列表；每个 option 使用稳定 ID，`aria-selected` 与高亮同步；活动项变化后调用 `scrollIntoView({ block: "nearest" })`；空结果不保留无效活动 ID。
- 验收标准：QuickOpen 组件测试 2/2；原有快速打开和窄窗口 20 项长列表 E2E 2/2；构建、lint、类型感知检查、格式检查和差异检查通过。剩余 #191 子切片保持开放。
- 涉及文件：`src/app/components/QuickOpenPalette.tsx`、`src/app/components/QuickOpenPalette.test.tsx`、`e2e/smoke.spec.ts`、`docs/UI-INTERACTION.md`、任务与交接文档。
- 依赖、风险与回滚：复用现有排序、`useModalBehavior` 和 listbox/option 语义，不新增依赖或迁移；ARIA 关联可能影响旧 DOM 定位器，保留现有 searchbox/option 角色和快捷键并以 E2E 锁定；回退 PR #424 即可恢复原高亮实现，不影响用户数据。
- 发布：普通 T2 UI 切片，不生成 Windows x64 安装包、Tag、Release、签名、`latest.json` 或镜像；纳入后续 v0.11.x 稳定批次。

## #191 目录 roving tabindex 与当前章节高亮（第 4 个子切片，当前，2026-09-03）

- 目标与用户价值：让非空文档目录共享一个可预测的 Tab 停靠点；键盘和读屏用户可用上下/Home/End 连续浏览并跳到章节，阅读滚动或目录跳转后当前章节仍清晰可辨。
- 非目标：不实现读屏 live announcement、焦点模式 Esc 互斥、目录折叠/展开或新的标题识别算法；不改变点击/鼠标、正文滚动算法、标签栏/文件树、文档内容、HTML、脚本、插件或发布链路。
- 基线与分支：基于已合并 `main@e9cde556e48957f270828159890522b52ef51f89` 创建项目内独立工作树；分支 `codex/outline-roving-2026-09-03`；Issue [#191](https://github.com/MY-moss/moyang_Reader/issues/191) 启动前未发现重复产品 PR；PR 待创建。
- 实现边界：目录声明 `tree/treeitem` 语义和标题层级；非空目录使用单一 roving `tabIndex=0`；上下/Home/End 移动焦点并复用现有中央正文定位；当前标题同步 `active`、`aria-current` 和 `aria-selected`，修饰键快捷键不被拦截。
- 验收标准：Outline 组件测试 5/5；浏览器键盘 E2E 1/1；构建、lint、类型感知检查、格式检查和差异检查通过；现有鼠标点击、正文导航和阅读高亮回归不变。
- 涉及文件：`src/app/components/Outline.tsx`、`src/app/components/Outline.test.tsx`、`e2e/smoke.spec.ts`、`docs/UI-INTERACTION.md`、任务与交接文档。
- 依赖、风险与回滚：复用现有 `TocItem`、`activeHeadingId`、中央滚动容器和减少动画策略，不新增依赖或迁移；阅读滚动时焦点与当前标题可暂时不同，标题刷新时 roving 项回退到有效项；回退本 PR 即恢复原目录链接行为，不影响 #424/#425/#426。
- 缓存处置：当前 Windows 开发环境已将旧 C 盘 Cargo target 迁移到 `D:\AI-moyang\本地阅读工具-build-cache`，源目录已清理；后续通过用户级 `MOYANG_BUILD_CACHE_DIR` 复用，并用清理器预览后回收可再生生成物。
- 发布：普通 T2 UI 切片，不生成 Windows x64 安装包、Tag、Release、签名、`latest.json` 或镜像；纳入后续 v0.11.x 稳定批次。

## #191 文件树 roving tabindex 与方向键导航（第 3 个子切片，已合并，2026-09-03）

- PR [#426](https://github.com/MY-moss/moyang_Reader/pull/426) 已 squash 合并为 `main@e9cde556e48957f270828159890522b52ef51f89`；Quality checks run `33653154436` 全部通过，Issue #191 保持开放。
- 文件树已具备 `tree/treeitem` 语义、单一 roving Tab 停靠点、上下/Home/End、文件夹 Left/Right 和虚拟窗口焦点恢复；缓存治理和工作树保护规则同步落地。

## #191 标签栏 roving tabindex 与方向键导航（第 2 个子切片，已合并，2026-09-02）

- 目标与用户价值：让已打开文档标签形成可预测的单 Tab 停靠点，方向键移动焦点并同步选择；键盘和读屏用户在多个文档间切换时不必逐个经过关闭按钮。
- 非目标：不实现文件树/目录 roving tabindex、读屏播报、焦点模式 Esc 互斥或其他 #191 子切片；不改变鼠标、中键关闭、拖拽排序、右键菜单、文档内容、HTML、脚本、插件或发布链路。
- 基线与分支：基于已合并 `main@a650f934429f8f19511dd6c72ef5b17541c694ff` 创建项目内独立工作树；分支 `codex/tabs-roving-2026-09-02`；Issue [#191](https://github.com/MY-moss/moyang_Reader/issues/191) 启动前重新核验未发现重复产品 PR；PR [#425](https://github.com/MY-moss/moyang_Reader/pull/425) 已 squash 合并为 `main@0783b27c314749a3e1e1b0371b92674a0a77a247`，Quality checks run `33642980506` 全部通过。
- 实现边界：标签栏声明水平方向；仅当前 roving 标签进入 Tab 顺序；ArrowLeft/ArrowRight 循环移动焦点并选择文档，Home/End 定位首尾；`aria-pressed` 与当前文档同步；关闭按钮仍是独立操作入口。
- 验收标准：Tabs 组件测试、双文档标签栏浏览器 E2E、构建、lint、类型感知检查和格式检查通过；右键菜单、中键关闭和拖拽排序回归不变。
- 依赖、风险与回滚：复用受控 `activePath`/`onSelect`、原生按钮焦点和现有菜单焦点契约，不新增依赖或迁移；异步切换/关闭时 roving 路径回退到有效标签；回退本切片 PR 即可恢复原标签焦点顺序，不影响用户数据。
- 发布：普通 T2 UI 切片，不生成 Windows x64 安装包、Tag、Release、签名、`latest.json` 或镜像；纳入后续 v0.11.x 稳定批次。

## #233 顶栏图标体系与窄窗口操作密度统一（已合并，2026-09-02）

- 目标与用户价值：建立零依赖、统一 stroke 风格的内联 SVG 图标，接入顶栏和编辑器中的撤销/重做、外部修改、侧栏、上下文、搜索、设置与导出，让操作更容易扫读并保持 900px 窄窗口无水平溢出。
- 非目标：不改变按钮动作、快捷键、命令注册或导出内容；不引入图标字体/组件库，不做完整主题或 CSS 令牌重构，不涉及 HTML 源码编辑、脚本、插件或发布链路。
- 基线与分支：基于已合并 `main@b7dc14358aee7025a83e86a7ba06d865914fddb1` 创建项目内独立工作树；分支 `codex/topbar-icons-2026-09-02`；Issue [#233](https://github.com/MY-moss/moyang_Reader/issues/233) 启动前未发现重复产品 PR；PR [#423](https://github.com/MY-moss/moyang_Reader/pull/423) 已 squash 合并为 `main@42337e840f2266f31715bee914630fc9b42cde1d`，Issue 已关闭。
- 实现边界：新增 `Icon` 共享组件，固定 `currentColor`、1.8px round stroke、装饰性 `aria-hidden`；高频按钮保留文字，撤销/重做和搜索结果操作保留稳定 aria 名称；复用现有 More 溢出策略。
- 验收标准：900px、深色和 forced-colors 浏览器 E2E 通过；设置/打印/下载入口在 More 中可见；全量单测、coverage、构建、lint、格式和类型感知检查通过。
- 涉及文件：`src/app/components/Icon.tsx` 及测试、`src/app/components/TopBar.tsx`、`src/app/components/EditorToolbar.tsx` 及测试、`src/app/styles.css`、`e2e/smoke.spec.ts` 及任务/交接文档。
- 依赖、风险与回滚：复用现有语义令牌和 #187 窄窗口布局，不新增依赖或迁移；图标仅作辅助并以布局 E2E 防止密度回退。回退 PR #423 可恢复原文字/Unicode 图标，不影响用户数据。
- 发布：普通 T2 UI 切片，不生成 Windows x64 安装包、Tag、Release、签名、`latest.json` 或镜像；纳入后续 v0.11.x 稳定批次。

## #370 周统计与本机记录清理（步骤 3/3，已合并，2026-09-02）

- 目标与用户价值：在侧栏展示本地周一至周日的阅读时长柱状摘要、去重阅读文档数和累计时长，并让用户明确清理本机阅读历史而不误删原文档或其他阅读状态。
- 非目标：不做目标/提醒、云同步、匿名上报、图表库、分钟级精度、历史趋势或按文档排行；不涉及 HTML 源码编辑、脚本、插件或发布链路。
- 基线与分支：基于已合并 `main@0ae85fc930a8a8f41db8f197734f5f1ef5d7db5a` 创建项目内独立 worktree；分支 `codex/reading-history-weekly-2026-09-02`；Issue [#370](https://github.com/MY-moss/moyang_Reader/issues/370) 未发现重复产品 PR；PR [#422](https://github.com/MY-moss/moyang_Reader/pull/422) 已 squash 合并为 `main@b7dc14358aee7025a83e86a7ba06d865914fddb1`，Issue 已以 `completed` 关闭。
- 实现边界：`reading-history.ts` 提供本地周聚合、时长格式化和安全清理；`ReadingHistoryPanel` 使用纯 CSS 7 日柱状条和空状态；应用内确认弹层只删除阅读历史键；追踪器写入后刷新侧栏摘要。
- 验收标准：7 个日桶按本地周一至周日显示，文档数按路径去重；组件单测覆盖摘要、空状态、清理请求和确认文案；浏览器 E2E 覆盖预置本机历史、打开确认、确认清理和归零；全量单测、coverage、构建、lint、格式和类型感知检查通过。
- 涉及文件：`src/app/reading-history.ts` 及测试、`src/app/App.tsx`、`src/app/components/WorkspacePanel.tsx` 及测试、`src/app/components/ReadingHistoryPanel.tsx` 及测试、`src/app/components/ReadingHistoryClearConfirmationDialog.tsx` 及测试、`src/app/styles.css`、`e2e/smoke.spec.ts`、相关任务与交接文档。
- 依赖与风险：依赖步骤 2 的本机 `dailySeconds` 数据、localStorage、Windows 路径规范化和统一 modal 行为；旧记录只有总秒数时不臆测归入本周，存储不可用时保持空状态；清理后未来阅读仍可重新记录。
- 验证：本地全量单测/覆盖率 92 文件、357 项、`84.17% / 72.80% / 91.09% / 88.40%`；周统计清理 E2E 1/1；远程 Quality checks run `33624287810` 全部通过（含浏览器、无障碍、桌面 smoke、发布预检和 Rust 门禁）。
- 回滚与发布：回退本切片 PR 即可移除周统计、清理入口和摘要刷新逻辑，不需数据迁移；普通 T1/T2 切片不生成 Windows x64 安装包、Tag、Release、签名、`latest.json` 或镜像，纳入稳定批次统一处理。

## #370 前台阅读可见性心跳（步骤 2/3，PR #421，2026-09-02）

- 目标与用户价值：当前打开的本机文档只在窗口可见且有焦点时累计整数阅读秒数；把总时长、最后阅读时间和近一年按本地日期的秒数留在本机，为下一步周统计提供数据，同时不把后台、失焦、最小化或锁屏时间算入。
- 非目标：不做周统计面板、清理入口、目标提醒、图表库、联网/匿名上报、分钟级精度、HTML 源码编辑、脚本、插件或发布资产；临时 `browser://` 文档不记录。
- 基线与分支：基于 `main@a75ff75e6600d82bb0223741b91a0b1309d9a07a` 的等价文件树；分支 `codex/reading-heartbeat-2026-09-02`；Issue [#370](https://github.com/MY-moss/moyang_Reader/issues/370)；PR [#421](https://github.com/MY-moss/moyang_Reader/pull/421) 已 squash 合并为 `main@0ae85fc930a8a8f41db8f197734f5f1ef5d7db5a`。
- 实现边界：新增 `src/app/reading-history.ts`，提供安全的 localStorage 规范化、路径合并、日期分桶和可注入时钟的追踪器；`App.tsx` 在文档路径变化时创建追踪器，并绑定 `visibilitychange`、focus/blur、pagehide/pageshow 与 cleanup；60 秒心跳只在默认前台状态累计。
- 验收标准：损坏 localStorage、非法时间、临时路径和持久化异常均不阻塞；pause/resume/stop 刷出已累计整数秒；测试模拟时钟验证前台心跳、隐藏/失焦隔离、恢复和跨大小写路径合并；构建和 lint 通过；Quality checks run `33619405983` 全部通过。
- 涉及文件：`src/app/reading-history.ts`、`src/app/reading-history.test.ts`、`src/app/App.tsx`、`vitest.config.ts`、`docs/UI-INTERACTION.md`、`docs/DEVELOPMENT-AUDIT.md`、`docs/NEXT.md`、`docs/AI-HANDOFF.md`、本文件、`tasks/plan.md`、`tasks/todo.md`。
- 依赖、风险与回滚：复用现有 documentState、localStorage、Windows 路径规范化和 React cleanup，不新增依赖/迁移；窗口事件延迟时心跳保守暂停，存储受限时保留未写入秒数；回退本 PR 即停止记录，不影响文档、最近打开或阅读位置。
- 发布：普通 T1 代码切片，不单独生成 Windows x64 安装包、Tag、Release、签名、`latest.json` 或镜像；稳定批次统一处理。

## #370 最近阅读时间语义（步骤 1/3，2026-09-02）

- 基线与范围：从已合并的 `main@f064b4621232dae9cbb292f6eaf200b5e3a3604a` 创建项目内工作树 `codex/reading-history-2026-09-02`；PR [#420](https://github.com/MY-moss/moyang_Reader/pull/420) 已上传；只处理最近打开记录的时间语义，不把阅读时长或统计面板并入本切片。
- 实现：`RecentFile.lastOpenedAt` 为可选 Unix 毫秒时间戳；最近记录上限为 50；有效时间按降序排列，旧数据保留并按原插入顺序安全回退；侧栏显示“刚刚/分钟/小时/天前”或“打开时间未知”；改名/移动保留时间字段。
- 非目标：不联网、不匿名上报、不做阅读心跳/累计时长/本周统计/清理入口，不引入图表库，不触碰 HTML 源码编辑、脚本、插件或发布资产。
- 验证：存储、便携设置和侧栏组件定向测试；浏览器启动列表 E2E 1/1；全量前端单测、TypeScript/Vite build、Lint、Prettier 和类型感知规则均已在本地通过。UI 但不涉及 Tauri runtime，因此不新增 Windows desktop E2E；远程 Quality checks run `33611774822` 已全部通过（17m23s），包含 Windows desktop smoke、依赖审计、发布预检和 Rust 测试。
- 回滚与发布：回退本切片 PR 即可恢复 12 条插入序列表，无数据迁移；本切片不生成安装包、Release、签名、`latest.json` 或 Cloudflare 镜像，纳入后续 v0.11.x 稳定批次。

## #366 统一确认弹层与未保存修改语义（PR #419，已合并，2026-09-02）

- 基线与范围：从合并后的 `main@45334c0b6cf9dc5f9b1bd39d2803b96181f0643e` 创建项目内工作树 `codex/confirm-dialog-2026-09-02`，提交 `52dd8d9` 已上传至 PR [#419](https://github.com/MY-moss/moyang_Reader/pull/419)，Quality checks run `33604977040` 全绿；PR 已 squash 合并为 `main@f064b4621232dae9cbb292f6eaf200b5e3a3604a`，Issue #366 已完成；只处理清空全部草稿和关闭未保存文档的确认语义，不处理 `window.prompt` 系列、其他确认入口或 HTML/插件/发布边界。
- 实现：新增应用内“清空全部草稿”弹层，复用 `useModalBehavior` 的初始焦点、Escape、Tab 和焦点归还；关闭弹层说明关闭前已保留草稿副本，提供“不保存退出”和“保存并退出”，后者先复用现有保存管线，保存失败不继续关闭；焦点守卫避免重叠弹层同时响应 Escape。
- 验收：清空全部确认不再调用 `window.confirm`；取消/确认只影响本机草稿快照；关闭文案区分两个退出后果；未引入运行时依赖或数据迁移。
- 涉及文件：`src/app/App.tsx`、`src/app/components/CloseConfirmationDialog.tsx` 及测试、`src/app/components/DraftClearAllConfirmationDialog.tsx` 及测试、`src/app/components/useModalBehavior.ts` 及测试、`e2e/smoke.spec.ts`、`desktop-e2e/smoke.e2e.mjs`、`docs/UI-INTERACTION.md` 及当前交接文档。
- 验证：组件/共享弹层定向测试 3 文件/8 项；前端全量单测 87 文件/343 项；全量浏览器 E2E 67/67；Windows desktop E2E 14/14；TypeScript/Vite build、全量 ESLint、Prettier、`git diff --check` 和类型感知规则 3/3 通过。桌面环境保留既有 `tauri-driver` 未安装、磁盘空间探针不可用和 mock-store 清理警告，均未影响退出码。
- 发布与回滚：本切片不生成 Windows x64 安装包、GitHub Release、签名文件、`latest.json` 或 Cloudflare 镜像；纳入后续 v0.11.x 稳定批次。回退本切片即可恢复原生清空确认和旧关闭文案，不需要数据迁移。

## #365 插入浮层焦点与图片浏览体验（已合并，2026-09-01）

- 基线：`origin/main@7f7a883a6db4c1494650df2b73f2d2fbba096afa`；分支：`codex/insert-popover-2026-09-01`；Issue：[#365](https://github.com/MY-moss/moyang_Reader/issues/365)；PR：[#408](https://github.com/MY-moss/moyang_Reader/pull/408)；合并提交：`4544c926a9a0485c1f02b6ac20f9982f81877da3`。
- 范围：插入类型 tab 使用 roving tabindex，支持方向键/Home/End；Escape 和外点关闭保留编辑器焦点；Windows 桌面版增加工作区图片选择，并将结果转换为当前文档相对 Markdown 路径。
- 保护：只允许已授权当前工作区内的图片和安全相对路径；拒绝工作区外、绝对和 `..` 路径；浏览器预览模式和图片选择失败显示明确反馈；不增加运行时依赖，不复制外部图片。
- 验证：定向单测 2 文件/6 项、变更文件 ESLint/Prettier、一次生产构建、Rust fmt/test/clippy、浏览器插入面板 E2E 2/2、Windows 桌面烟测和远程 Quality checks run `33526998019` 全部通过。
- 发布/回滚：不单独生成 Windows x64 安装包、Tag、Release 或 Cloudflare 镜像，纳入 `v0.11.0` 稳定批次；回退 PR #408，无数据迁移；Issue #365 已以 `completed` 关闭。

## #372 设置备份 v2（已合并，2026-09-01）

- 基线：`main@0dd24b3cddda4c2ec150704ef1129a8723e5446a`；分支：`codex/portable-reading-positions-2026-09-01`；Issue：[#372](https://github.com/MY-moss/moyang_Reader/issues/372)；PR：[#402](https://github.com/MY-moss/moyang_Reader/pull/402)；合并提交：`d93c992474db0e5e8ab0f04d68044db1ab774700`。
- 目标：便携设置导出/导入升级到 v2，包含最多 32 条阅读位置和本机书签；v1 备份可继续导入且不覆盖当前本机阅读状态。
- 实现边界：统一 `storage.ts` 的阅读位置规范化、大小写不敏感去重和批量读写；`App.tsx` 仅在 v2 导入时恢复阅读位置/书签；临时 `browser://` 书签不导出。
- 验收：v1/v2 解析兼容、非法记录过滤、32 条裁剪、读写 round-trip 与导入提示均有覆盖；不复制文档正文、草稿或 AI 历史，不改变 Markdown 真源。
- 当前结果：定向测试 2 文件/17 项、TypeScript、Lint、格式检查和差异检查通过；远程 Quality checks run `33474094871` 通过，Issue #372 已标记 `completed`。
- 发布/回滚：不单独生成 Windows x64 安装包、Tag、Release 或 Cloudflare 镜像，纳入 `v0.11.0` 稳定批次；回退本切片不需要迁移。

## #368 选中文本高亮批注第二切片（已合并，2026-09-01）

- 基线：`origin/main@9f5f7934f0fdfcb2ac0b265eb29f574c9ba2c583`；分支：`codex/annotation-highlight-2026-09-01`；Issue：[#368](https://github.com/MY-moss/moyang_Reader/issues/368)；PR：[#399](https://github.com/MY-moss/moyang_Reader/pull/399)；合并提交：`871d6032336036b406109126c26f80bf831463bc`。
- 范围：阅读模式选中文本后通过右键创建高亮/备注；批注以引文、前后文和偏移锚点保存到已授权工作区 `.moyang/annotations.json`；右栏批注页签支持查看、定位和删除；CSS Custom Highlight 不可用时使用 DOM 回退。
- 保持：Markdown 是唯一正文真源；未知/失配引文保留并显示“待定位”；设置关闭只停止读取和渲染，不删除 sidecar；不支持 DOCX/PDF/图片标注、编辑器内标注、尾注导出和跨设备同步。
- 验证：前端全量 83 文件/323 项、TypeScript、Lint、格式检查、一次生产构建、Rust 全量 52 项/clippy、浏览器批注 E2E 1/1、Windows desktop smoke 14/14、发布预检和构建产物检查均通过。
- 发布/回滚：本切片不单独生成 Windows x64 安装包、Tag、Release 或 Cloudflare 镜像，纳入 `v0.11.0` 稳定批次；回退 PR #399，无数据迁移。CI Quality checks run `33469013579` 全部通过，Issue #368 已标记 `completed`；本轮交接完成后停止。

## #368 文档书签第一切片（已合并，2026-09-01）

- 基线：`origin/main@eb225d0834bdba363d45a66f4abaadfebd45c5bc`；分支：`codex/document-bookmarks-2026-09-01`；Issue：[#368](https://github.com/MY-moss/moyang_Reader/issues/368)；PR：[#397](https://github.com/MY-moss/moyang_Reader/pull/397)；合并提交：`fc51db210cf2142f4a317e713516dd39c6454edf`。
- 范围：新增 `bookmarks.ts` 本机 localStorage 边界和 `BookmarksPanel`；阅读模式正文右键按文档/标题锚点添加或移除；右栏新增“书签”页签，支持列出、跳转、删除和当前阅读库外状态提示；设置/旧存储接受新页签值。
- 数据边界：书签只保存 `{path, headingId?, quote?, note?, createdAt}`，不复制正文、不写回 Markdown、不做选中文本批注；浏览器 `browser://` 定位只在当前会话生效，不进入持久化存储。
- 验证：书签、面板、右键菜单、存储和设置定向测试 5 文件/27 项；快速打开与书签闭环浏览器 E2E 2/2；TypeScript、Lint、格式检查、`git diff --check` 和一次生产构建通过。
- 发布/回滚：不单独生成 Windows x64 安装包、Tag、Release 或 Cloudflare 镜像，纳入 `v0.11.0` 稳定批次；回退 PR #397，无数据迁移。Issue #368 保持 open，需重新检查 Issues/Ready 后再决定第二切片。
- 结果：Quality checks run `33460590462`、Frontend dependency audit run `33460590363` 均成功；PR #397 已 squash 合并。其间同步修正了 feature-branch PR 的 CI 触发路径，并调整现有读者右键菜单 E2E 的焦点断言以覆盖新增书签项。

## #367 文档级跳转历史与返回上一文档（已合并，2026-09-01）

- 基线：`origin/main@c80d966f080772bb51dc60ea01cdffeafeff92ba`；分支：`codex/document-navigation-history-2026-09-01`；Issue：[#367](https://github.com/MY-moss/moyang_Reader/issues/367)；PR：[#395](https://github.com/MY-moss/moyang_Reader/pull/395)；合并提交：`556a6fa98c7a5afd69aa88d4d2167ce5a90f6f8b`。
- 目标：沿标签页、文件树、快速打开或双链切换文档后，用 `Ctrl+Alt+←` 或命令面板返回上一文档；访问历史只保存在当前会话，不写入设置或用户文件。
- 实现：新增 `navigation-history.ts`，支持 push/back/forward、Windows 路径去重和最多 50 条历史；直接打开建立新基准，切换成功后才压栈，打开失败或未保存确认取消时保持历史不变。
- 验证：导航历史单测 5/5、TypeScript、Lint、格式检查、一次前端生产构建、导航快捷键与命令面板浏览器 E2E 1/1、`git diff --check` 通过。
- 发布/回滚：不单独生成 Windows x64 安装包、Tag、Release 或 Cloudflare 镜像，合入后纳入 `v0.11.0`；回退 PR #395 即可，无数据迁移。
- 结果：Quality checks run `33416594395` 全部通过，PR #395 已合并，Issue #367 已以 `completed` 关闭；`NEXT.md` 和 `AI-HANDOFF.md` 已切换到 #368。在交接完成前不自动开始下一项。

## #362 交互与渲染微成本包（已合并，2026-08-31）

- 基线：`origin/main@28bf09d88f3eb379897c31adbdeda5ef426b2adc`；分支：`codex/perf-micro-cost-2026-08-31`；Issue：[#362](https://github.com/MY-moss/moyang_Reader/issues/362)；PR：[#392](https://github.com/MY-moss/moyang_Reader/pull/392)；合并提交：`c936666043d32ed1a4a1eec9312684994636034a`。
- 范围：拖动分隔条只更新 CSS 变量，结束拖动才提交 React 状态和持久化；草稿存储复用原始序列化内容的解析结果并回传最新快照列表；差异弹层对稳定输入使用 `useMemo`。
- 保持：面板边界、键盘调宽、草稿 storage key、恢复语义和 Markdown 真源不变；不引入数据库、后台服务或新运行时依赖。
- 验证：定向单测 17/17、全量前端单测 76 文件/300 项、TypeScript、Lint、格式检查、一次生产构建和侧栏拖拽浏览器 E2E 1/1 通过；草稿 parse 回归探针为 1 次；远程 Quality checks run `33403555956` 重跑成功，Windows desktop smoke、依赖审计、发布检查和 Rust 门禁均通过。
- 首轮远程桌面基准只有一次 318.5ms renderer gap，后两轮和取消场景均低于门槛；重跑未复现，未放宽性能阈值或混入无关修复。
- 发布/回滚：不单独生成 Windows x64 安装包、Tag、Release 或镜像，合入后纳入 `v0.11.0`；回退 PR #392 即可，无数据迁移。
- 结果：Issue #362 已以 `completed` 关闭；当前唯一下一项见 [`../NEXT.md`](../NEXT.md) 的 #367，完成本次交接后停止。

## #361 暗色主题 accent 按钮对比度修复（已合并，2026-08-31）

- 基线：`origin/main@0f11b602f675e5c5d8a62d27931b6595939f208a`；分支：`codex/dark-accent-contrast-2026-08-31`；PR [#390](https://github.com/MY-moss/moyang_Reader/pull/390) 已 squash 合并为 `main@41acf808a54683e9ed4b2f7a1d15cdc132c8629d`。
- 范围：实心主按钮、编辑器插入按钮和插入提交按钮增加独立对比度令牌；自动/显式暗色主题覆盖普通、hover、focus；Windows 强制高对比度使用系统按钮色。
- 验证：`e2e/a11y.spec.ts` 7/7；Lint、格式检查、`git diff --check` 和一次前端生产构建通过；远程 Quality checks run `33392327386` 成功。
- 边界：不改变 Markdown、编辑器行为、导出、更新器、用户数据或运行时依赖；不单独生成安装包、Tag、Release 或镜像，纳入 `v0.11.0` 稳定 Windows x64 批次。
- 结果：Issue #361 已由维护者明确以 `completed` 关闭；回滚为回退 PR #390，无数据迁移。

## #359 撤销历史分组与内存上限（已合并，2026-08-31）

- 基线：`origin/main@4a60c61fe5ec0b6adc760f177af4f166275984e`；分支：`codex/editor-undo-granularity-2026-08-31`；Issue：[#359](https://github.com/MY-moss/moyang_Reader/issues/359)；PR：[#388](https://github.com/MY-moss/moyang_Reader/pull/388)；合并提交：`5f8fba3a37d429aa052add4543832b096ee28da5`。
- 范围：编辑器连续输入在 400ms 窗口内合并为一个撤销组；历史最多 100 条，past/future 快照合计最多 8 MiB 的 UTF-16 估算值；程序化更新保持原子边界。
- 保持：Markdown 真源、源码/WYSIWYG 共用应用级历史、Ctrl+Z/Ctrl+Y、保存和切换文档语义均不变；不改解析、草稿恢复、备份、导出或发布流程。
- 验证：定向编辑历史单测 6/6、TypeScript 检查、一次生产构建、源码快速输入浏览器 E2E 1/1、跨模式撤销/重做回归 E2E 1/1，以及最终 lint/format/diff 检查均已通过；远程 Quality checks run `33386166171` 成功，Issue 已关闭。
- 发布/回滚：不单独生成安装包、Tag、Release 或镜像，合入后纳入 `v0.11.0`；回退本切片提交即可，不需要数据迁移。

## #369 回收站删除与保存上一版本保护（已合并，2026-08-31）

- 基线：`origin/main@16180304cf80d713ae411015b3832fa4338e96d3`；分支：`codex/recycle-bin-backup-2026-08-31`；PR [#386](https://github.com/MY-moss/moyang_Reader/pull/386) 已 squash 合并为 `main@7aaf2bffe887ec7967c6b6fb44391fac657acbd7`。
- 目标：删除文件/文件夹使用 Windows 回收站语义；文本保存成功后保留最近一份上一版本；恢复前展示当前版本与上一版本差异，恢复只进入编辑区。
- 实现：`SHFileOperationW + FOF_ALLOWUNDO`，兼容 Windows 扩展长度路径；滚动 `.moyang.bak`；上一版本读取桥接、提示、差异对话框和顶部入口；删除文本文件时将无主备份一并处理。
- 验证：前端定向 3 文件/14 项、Rust 编译/格式/备份与删除定向测试、工作流 13/13、Windows desktop smoke 14/14、Lint、格式、构建和 `git diff --check` 通过；远程 Quality checks `33367813186` 与 Rust audit `33367921971` 均成功。
- 限制：特殊路径/网络盘/禁用回收站可能无法接收内容，失败时保留原文件；仅保留最近一份备份；发布前仍需确认 Explorer 还原路径。
- 发布/回滚：不单独生成安装包、Tag、Release 或镜像，合入后纳入 `v0.11.0`；回退本切片 PR，不需要数据迁移。
- 结果：Issue #369 已自动以 `completed` 关闭；准确合并 SHA 已写入本节、`NEXT.md`、`AI-HANDOFF.md` 和 `ROADMAP.md`；当时下一唯一 READY 事项为 #359。

## 工作区构建缓存回归修复（2026-08-31）

- 背景：#375 的空间治理在后续主线整理时丢失了 Tauri/Cargo 外置缓存包装；直接执行桌面构建会重新生成项目内 `src-tauri/target`，并可能在多个工作树中重复占用数 GB。
- 分支：`codex/build-cache-guard-2026-08-31`；基于合并 #380 后的 `main@52acd223d7ca0fb251f143bfc02820eecc88337e`。
- 范围：恢复 `npm run desktop`、`npm run tauri -- <args>` 和 `npm run rust -- <args>` 包装；桌面 E2E、CI Rust 检查和发布前 Rust 检查统一使用外置缓存；仓库内或缓存目录误配置到项目时自动重定向。
- 验证：工作流/路径测试 11/11、Cargo 包装 `--version`、Tauri 包装 `--version`、目标路径检查、目标脚本 ESLint、Prettier 和 `git diff --check` 通过；未生成 `src-tauri/target`，不创建安装包、Tag、Release 或镜像。
- 回滚：回退本切片提交；被清理的 `src-tauri/target` 仅为可再生构建产物，不触碰源码、文档、用户笔记或 Git 历史。

## #358 插入浮层跟随光标/视口（已合并，2026-08-31）

- 基线：`main@52acd223d7ca0fb251f143bfc02820eecc88337e`；分支：`codex/insert-popover-viewport-2026-08-31`；PR [#380](https://github.com/MY-moss/moyang_Reader/pull/380) 已 squash 合并。
- 范围：插入链接、双链、图片和表格面板通过 portal 使用视口内 fixed 定位；锚点来自 Milkdown/CodeMirror 光标；输入框使用 `preventScroll`；编辑器滚动关闭过期面板；取消恢复原选区和阅读位置。
- 验证：定位单测 4/4；相关回归单测 13/13；lint、format、TypeScript/Vite build；长文档定位 E2E 1/1；链接、图片和表格回归 E2E 2/2；远程 Quality checks 全部成功。
- 边界：不改变 Markdown 输出、插入语义、右键菜单、持久化、更新器或发布资产；不单独创建安装包、Tag、Release 或镜像。
- 状态：Issue #358 已以 `completed` 关闭；下一项为 PR #379 的构建缓存防膨胀修复。

## 规划与快速交接体系（2026-08-30）

- 目标：把 510 行、包含多个冲突下一步的历史交接拆成唯一执行契约、当前状态摘要和按版本归档。
- 分支：`codex/v0.11-roadmap-handoff-2026-08-29`。
- PR：[#340](https://github.com/MY-moss/moyang_Reader/pull/340)。
- 范围：文档、PR 模板、GitHub milestone 与 Issue 规划；无产品代码、无版本变化、无 Release。
- 结果：建立 `docs/NEXT.md`，压缩 `AI-HANDOFF.md`，归档旧记录，并把 v0.11 六阶段顺序同步到仓库与 GitHub。
- 验证：Prettier、Markdown 相对链接、`git diff --check`、唯一下一步和远端文件范围检查均通过；CI 结果见 PR #340。
- 下一步：只执行 `docs/NEXT.md` 中的 #87 最终 Windows 批量 DOCX 矩阵。

## #87 批量 DOCX 最终 Windows 矩阵（2026-08-30）

- 基线：`origin/main@628e5c3c23a942bd893fd2a9bbff2a66ddcac2bc`；分支：`codex/batch-export-final-matrix-2026-08-30`。
- PR：[#341](https://github.com/MY-moss/moyang_Reader/pull/341)；Issue：[#87](https://github.com/MY-moss/moyang_Reader/issues/87)。
- 范围：真实 Tauri desktop smoke 的 96 篇矩阵，覆盖重复图片、独立图片、长文本、复杂表格和嵌套 HTML；连续 3 轮成功导出，并覆盖取消与目标目录失败清理。
- 结果：三轮 renderer 最大间隔 77/80/76ms，上下文交互 3/6/6ms；取消延迟 50ms；成功、取消产物均为含 `word/document.xml` 的有效 DOCX；所有临时文件清理为 0；Working Set 趋势均非单调增长。
- 本地验证：定向导出 37、全量单测 265、覆盖率、lint、format、build、浏览器 E2E 48、Windows desktop E2E 12、release checks、Actions 固定检查、npm audit 0 vulnerabilities、Rust fmt/clippy/tests 51 均通过。
- 变更边界：未修改生产导出语义；无安全、权限、发布或数据迁移风险；未创建安装包、Tag 或 Release。
- 回滚：回退 PR #341；下一唯一任务已切换为 #234，等待 PR/CI 完成后再开始。

## #234 设置反馈统一通知层（2026-08-30）

- 基线：`origin/main@33c798cbf34df74b9e0a46f324c4303226f8b36f0`；分支：`codex/notification-layer-2026-08-30`；独立 worktree。
- PR：[#342](https://github.com/MY-moss/moyang_Reader/pull/342)；Issue：[#234](https://github.com/MY-moss/moyang_Reader/issues/234)。
- 范围：统一设置/编辑器反馈与 `UpdateNotice` 的固定右上角视口；最多 3 条、FIFO、单条关闭；info/success 六秒自动关闭；error/action 常驻；不挤动正文或 body 布局。
- 结果：新增通知队列与视口组件；设置成功/失败、导入导出、语言/偏好反馈统一接入；UpdateNotice 保留原动作和进度并共用视口；增加状态/警告语义、键盘关闭和窄窗口布局回归。
- 本地验证：270 个单测、50 条浏览器 E2E、Windows desktop E2E 12/12、lint、format、build 全部通过。
- CI：`sha=c37627628bf9916b00a31961a672b68827a6139e workflow=CI/Quality checks run_id=33290796215 conclusion=success last_changed_at=2026-08-30T03:49:37Z next_action=merge PR #342`。
- 变更边界：T2；无安全、权限、发布资产、安装包、Tag、Release 或数据迁移影响。回滚为回退 PR #342。
- 下一唯一任务：#189 类型感知 TypeScript/ESLint 门禁；不自动开始。

## #189 TypeScript/ESLint 类型感知异步门禁（2026-08-30）

- 基线：远程 `main@4ca46c52e92bce6353667f0632db40613df4424b`；分支：`codex/typescript-eslint-gates-2026-08-30`。
- 范围：仅 `src` 启用 `no-floating-promises`、`await-thenable`、`no-misused-promises`；脚本和 desktop-e2e 保持非类型感知配置；CI 增加 3/3 规则探针。
- Fallout：首次评估发现 13 处测试中同步 `act` 回调把返回值暴露为未处理 Promise，已改为无返回值 block callback；业务源码无新增 fallout。
- 验证：全量前端测试 68 文件/270 项、TypeScript build、lint、format、类型规则探针、发布预检、Actions 固定检查、Rust fmt/clippy/tests 51 全部通过；远程 Quality checks 结果以 PR 为准。
- 变更边界：T2；无用户功能、数据迁移、导出、更新器、安装包、Tag、Release 或镜像影响。回滚为回退本切片 PR。
- 下一唯一任务：#301 文件拖放状态和失败反馈；不自动开始。

## #346 草稿恢复当前版本核对（2026-08-30）

- 基线：`origin/main@3b82408d67772f75cb75ec5eca702e8ec014abc1`；分支：`codex/draft-compare-2026-08-30`；独立 worktree。
- Issue：[#346](https://github.com/MY-moss/moyang_Reader/issues/346)。范围是恢复前读取当前磁盘文件、明确来源和差异、在读取中/失败时阻止恢复；不做版本历史、云同步或三方合并。
- 结果：恢复中心的非当前文档不再把旧 `baseSource` 冒充当前版本；浏览器回退显式标注；增加来源卡片、差异统计、换行等价判断、重试和异步过期保护；恢复仍须显式保存。
- 验证：定向单测 4 文件/13 项、TypeScript build、format、git diff 检查、生产构建、浏览器恢复中心 E2E 1/1、Windows desktop targeted smoke 1/1 通过。
- 发布/回滚：不创建安装包、Tag、Release 或镜像；回退本切片 PR 即可恢复旧行为。PR #347 已合并并关闭 #346，下一唯一事项为 #299。

## #299 右键菜单键盘导航与焦点归还（2026-08-30）

- 基线：远程 `main@0e85cbc0d9f6507f8dc0fcf08f748cac77d7b9cd` 的等价文件树；分支：`codex/context-menu-focus-2026-08-30`；独立 worktree。
- Issue：[#299](https://github.com/MY-moss/moyang_Reader/issues/299)。范围是共享右键菜单的 Tab/Shift+Tab 循环、方向键稳定导航、关闭后的焦点归还和失效触发器安全回退。
- 结果：文件树、标签页、阅读区、WYSIWYG 和源码编辑器的鼠标/Context Menu/Shift+F10 入口共用焦点契约；父组件重渲染不会重置菜单位置；菜单项关闭、Escape 和外点关闭均尝试恢复触发元素。
- 验证：共享 ContextMenu 单测、浏览器键盘 E2E、Windows desktop 文件树 targeted smoke、全量单测、lint、format、TypeScript build 和生产 build 均通过；桌面回归优先发送真实 WebDriver Escape，嵌入 Tauri driver 未转发 keydown 时使用同一焦点目标的受控 DOM 回退，driver/mock-store 的既有环境警告不影响通过结果。
- 发布/回滚：不创建安装包、Tag、Release 或镜像；回退本切片 PR 即可恢复旧焦点行为，不涉及数据迁移。
- 完成后唯一下一步：合并并关闭 #299 后执行 [`NEXT.md`](../NEXT.md) 中的 #119；不自动开始下一项。

## #119 axe/WCAG AA Windows UI 基线（2026-08-30）

- 基线：远程 `main@ee84be9949f71f2745118b52a89d889ce170106c`；分支：`codex/a11y-baseline-2026-08-30`；独立 worktree。
- Issue：[#119](https://github.com/MY-moss/moyang_Reader/issues/119)；PR：[#350](https://github.com/MY-moss/moyang_Reader/pull/350)。
- 范围：空状态、阅读、快速打开和设置四个浏览器入口分别运行 critical/serious axe；浅色/深色关键文字令牌执行 WCAG AA 计算；Windows `forced-colors` 运行 axe 和实际 CSS 颜色探针；新增 Narrator/NVDA 手动冒烟清单。
- 结果：修正浅色辅助文字、暖色状态、深色危险状态和面板 kicker 的对比度；新增 `test:e2e:a11y` 定向入口和高对比度 CSS 回退；无新增运行时依赖、跨平台范围或用户文档同步。
- 验证：本地 `npm run lint`、`npm run format:check`、`npm run build`、`npm run test:e2e:a11y -- --reporter=line --workers=1`（6/6）通过；远程 `CI` 对 `c0bca3de57e00cee3bbe59b8956b9a3c9952b856` 的 `run_id=33320052388` 于 `2026-08-30T15:43:13Z` 成功。
- 发布/回滚：不创建安装包、Tag、Release 或镜像；回退 PR #350 即可恢复原主题令牌和测试范围。真实 Narrator/NVDA 抽查保留在发布前清单，不声称自动化替代读屏认证。
- 完成后唯一下一步：#190 首屏加载与大文档挂载；不自动开始。

## #190 首屏按需加载与大文档渐进挂载（2026-08-31）

- 基线：`main@aa7c2ec8809563065545c6c828017cb9a32a1477`；分支：`codex/perf-first-render-2026-08-31`；独立 worktree。
- PR：[#351](https://github.com/MY-moss/moyang_Reader/pull/351)；Issue：[#190](https://github.com/MY-moss/moyang_Reader/issues/190)；合并提交：`e7a08d655d952201eeae58e77624284cdf52bf1d`。
- 范围：普通文档按需加载 KaTeX CSS；大 HTML 先挂载首块，再按帧渐进挂载；搜索、目录和锚点跳转在需要时揭示剩余内容；构建产物检查确认 KaTeX CSS 已从入口 CSS 分离。
- 非目标：未改变 Markdown 解析、双链、导出、编辑器语义或 CodeMirror/Milkdown 懒加载；未引入大型运行时、数据迁移、更新器或跨平台构建。
- 验证：定向单测 11/11、lint、类型规则探针 3/3、format、一次生产 build、构建产物检查、性能浏览器 E2E 2/2 和相关回归 E2E 4/4 通过；远程 Quality checks `run_id=33322595846`、依赖审计 `run_id=33322611814` 均成功。
- Issue 状态：#190 已由 PR #351 自动以 completed 关闭；PR 无遗留冲突。
- 发布/回滚：本切片不单独生成 Windows 安装包、Tag、Release 或镜像，结果纳入 v0.11.0；回退 PR #351 即可恢复原渲染挂载路径。
- 下一唯一任务：#172 reduced-motion 程序化滚动，详见 [`NEXT.md`](../NEXT.md)；不自动开始。

## #172 reduced-motion 程序化滚动（2026-08-31）

- Issue：[#172](https://github.com/MY-moss/moyang_Reader/issues/172)；PR：[#374](https://github.com/MY-moss/moyang_Reader/pull/374)；合并提交：`c187edcf39798b16d9610b5b8fdda6e22532086c`。
- 范围：程序化滚动统一读取 Windows `prefers-reduced-motion`，普通模式保持平滑滚动；不改变编辑、导出、保存或持久化语义。
- 验证与状态：相关单测、lint、format、TypeScript 和浏览器回归已通过；Issue #172 已关闭；不单独生成安装包、Tag、Release 或镜像。
- 回滚：回退 PR #374；下一项在交接同步后重新核验。

## #375 工作区空间治理（2026-08-31）

- Issue：工程维护切片；PR：[#375](https://github.com/MY-moss/moyang_Reader/pull/375)；合并提交：`c3f5c8ce1967f2649a47337ca699aedca48fd1e8`。
- 范围：忽略可再生缓存，提供安全清理器和工作树依赖 junction 复用；只清理明确生成物，不触碰用户笔记、源码或真实主工作区 `node_modules`。
- 验证与结果：清理器/工作树辅助脚本测试、CI Quality checks 和 Rust audit 已通过；本地实际生成物已清理；不生成安装包、Tag、Release 或镜像。
- 回滚：回退 PR #375；若回收工作树，禁止对含 junction 或脏改动的工作树执行强制移除。

## 当前交接

- 当前主线：`main@41acf808a54683e9ed4b2f7a1d15cdc132c8629d`；稳定版本：`v0.10.13`。
- 当前唯一 READY：#362 交互与渲染微成本包，契约见 [`../NEXT.md`](../NEXT.md)。
- 发布边界：#359、#361 等 T2 编辑/视觉切片不单独生成安装包；待 v0.11.0 稳定批次再统一发布。

## #357 右键菜单 viewport 定位（2026-08-31）

- Issue：[#357](https://github.com/MY-moss/moyang_Reader/issues/357)；PR：[#377](https://github.com/MY-moss/moyang_Reader/pull/377)；合并提交：`c32c34991b25f11cf4890ad793dbdbc065e46872`。
- 范围：共享 `ContextMenu` 通过 React portal 渲染到 `document.body`，脱离 `.content-area` containing block；保留视口坐标、边缘 clamp、键盘导航和焦点归还。
- 验证与状态：相关单测 5 文件/16 项、lint、一次前端构建、格式检查和右键定位/正文滚动浏览器 E2E 1/1 通过；远程 Quality checks `run_id=33331543322`、`job_id=99310836649` 成功；Issue #357 已以 completed 关闭。
- 发布/回滚：不单独生成安装包、Tag、Release 或镜像；纳入 v0.11.0；回退 PR #377。
