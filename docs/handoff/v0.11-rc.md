# v0.11 RC / 发布预检记录

日期：2026-09-18

历史切片记录继续保存在 [`v0.11.md`](v0.11.md)；本文件记录 A13 的最终 RC 预检结果。

## 结果

- 当前稳定发布基线仍为 `v0.10.14`；本次只完成 RC 预检和兼容性收口，不升版本、不创建 Release、不生成新的安装包或签名。
- 仓库内检查通过：`npm run release:check -- --version=v0.10.14`、`npm run release:status -- --version=v0.10.14`、`npm run test:release`（22/22）和 `npm run test:workflow`（33/33）。
- PR [#486](https://github.com/MY-moss/moyang_Reader/pull/486) 的 Quality checks [run 35302296027](https://github.com/MY-moss/moyang_Reader/actions/runs/35302296027) 全绿，包含前端覆盖率/构建、Playwright 与 a11y、Windows desktop correctness smoke、Rust format/clippy/tests 和发布预检。
- v0.10.14 GitHub Release 为非 Draft、非 Pre-release，安装包、`.sig`、`latest.json` 均已上传；GitHub 三项资产在线返回 HTTP 200，Release API digest 与 `docs/release-status.json` 记录一致；公开 Cloudflare 镜像的 manifest、版本化安装包和 `.sig` 也均返回 HTTP 200，版本与大小一致。
- 更新器权威顺序已核对为 GitHub Release → Cloudflare Pages 镜像；配置测试和文档检查都阻止镜像优先回退。

## BLOCKED_EXTERNAL

- #241：旧版本 Windows 安装、检查更新、下载、替换、重启的完整闭环仍缺少可重复的旧版安装环境；仓库内 smoke 不能替代这项实机证据。
- #51：当前没有 NSIS Authenticode 证书；Tauri updater `.sig` 和 SHA-256 核验继续有效，但不能写成 Authenticode 已完成。
- 静态 Cloudflare 镜像 workflow 仍缺少 `CLOUDFLARE_API_TOKEN` / `CLOUDFLARE_ACCOUNT_ID`；公开动态镜像可访问，不等同于静态部署已完成。

上述阻塞只限制正式发布闭环的外部子项，不阻塞已完成的仓库内代码、测试和文档收口。
