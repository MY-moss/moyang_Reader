# v0.11.0 Release 发布记录

日期：2026-09-18

## 当前状态

- 当前稳定 Release 已为 `v0.11.0`；`v0.10.14` 保留为旧版本自动更新验证基线。
- 本候选把 `package.json`、`src-tauri/Cargo.toml`、`src-tauri/Cargo.lock` 和 `src-tauri/tauri.conf.json` 统一到 `0.11.0`。
- `v0.11.0` tag、GitHub Release、NSIS 安装包、`.sig` 和 `latest.json` 已由 Release run `35361595825` 生成并在线核验；详细大小和 SHA-256 见 `docs/release-status.json`。
- 主线 Rust 依赖审计发现 `rustls 0.23.43` 的新安全公告，已将锁文件更新到修复版 `0.23.45`；不改变应用 Rust 代码或 IPC 行为。
- Release workflow 的主发布 job 与完整质量门禁通过；mirror job 因缺少 Cloudflare Secrets 未执行静态镜像部署。

## 待完成发布步骤

1. 已合并版本候选提交并确认 `main` CI 全绿。
2. 已推送 `v0.11.0` tag，Release workflow 已生成 NSIS 安装包、updater `.sig`、`latest.json` 和 GitHub Release。
3. 已在线核对 GitHub Release 的版本、HTTP 下载、文件大小、SHA-256、manifest 和签名文件；Cloudflare 镜像因 Secrets 缺失保持 `BLOCKED_EXTERNAL`。
4. 记录 `v0.10.14 → v0.11.0` 旧版本自动更新实机结果；当前缺少可重复环境，保持 `BLOCKED_EXTERNAL`。

## 已知外部限制

- Cloudflare 静态镜像仍依赖 `CLOUDFLARE_API_TOKEN` / `CLOUDFLARE_ACCOUNT_ID`。
- 当前没有 NSIS Authenticode 证书；updater `.sig` 不替代 Authenticode 结论。
