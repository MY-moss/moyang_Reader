# v0.11.0 Release Candidate 准备记录

日期：2026-09-18

## 当前状态

- 当前稳定 Release 仍为 `v0.10.14`。
- 本候选把 `package.json`、`src-tauri/Cargo.toml`、`src-tauri/Cargo.lock` 和 `src-tauri/tauri.conf.json` 统一到 `0.11.0`。
- `docs/release-status.json` 暂记为 `planned`；安装包、`.sig`、`latest.json`、tag 和 GitHub Release 尚未生成。
- 候选提交合并并通过主线质量门禁后，仍需维护者明确授权推送 `v0.11.0` tag，才能触发 Windows Release workflow。

## 待完成发布步骤

1. 合并版本候选提交并确认 `main` CI 全绿。
2. 推送 `v0.11.0` tag，等待 Windows Release workflow 生成 NSIS 安装包、updater `.sig`、`latest.json` 和 GitHub Release。
3. 在线核对 GitHub Release 与 Cloudflare 镜像的版本、HTTP 状态、文件大小、SHA-256 和签名。
4. 记录 `v0.10.14 → v0.11.0` 旧版本自动更新实机结果；缺少可重复环境时保持 `BLOCKED_EXTERNAL`。

## 已知外部限制

- Cloudflare 静态镜像仍依赖 `CLOUDFLARE_API_TOKEN` / `CLOUDFLARE_ACCOUNT_ID`。
- 当前没有 NSIS Authenticode 证书；updater `.sig` 不替代 Authenticode 结论。
