name: Frontend dependency audit

on:
  pull_request:
    paths:
      - "package.json"
      - "package-lock.json"
      - "scripts/check-action-pins.mjs"
      - "scripts/check-action-pins.test.mjs"
      - "scripts/dev-dependency-audit.mjs"
      - "scripts/dev-dependency-audit.test.mjs"
      - "scripts/dev-audit-exceptions.json"
      - "docs/security/DEV-DEPENDENCY-RISK-ACCEPTANCE.md"
      - ".github/workflows/**"
  push:
    branches: [main]
    paths:
      - "package.json"
      - "package-lock.json"
      - "scripts/check-action-pins.mjs"
      - "scripts/check-action-pins.test.mjs"
      - "scripts/dev-dependency-audit.mjs"
      - "scripts/dev-dependency-audit.test.mjs"
      - "scripts/dev-audit-exceptions.json"
      - "docs/security/DEV-DEPENDENCY-RISK-ACCEPTANCE.md"
      - ".github/workflows/**"
  schedule:
    - cron: "17 4 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    name: Frontend dependency audit
    runs-on: ubuntu-latest
    env:
      NPM_CONFIG_REGISTRY: https://registry.npmjs.org
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 22
          cache: npm

      - name: Verify pinned GitHub Actions
        run: node scripts/check-action-pins.mjs

      - name: Install dependencies without lifecycle scripts
        run: npm ci --ignore-scripts --no-audit --no-fund

      - name: Audit production dependencies
        run: npm audit --omit=dev --audit-level=high

      - name: Audit development dependencies against expiring exceptions
        run: npm run audit:dev
