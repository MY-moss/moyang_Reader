## 任务与范围

- 关联 Issue/反馈：
- 优先级：Must / Should / Could / Won't
- 风险级别：T0 / T1 / T2 / T3
- 这个 PR 解决的用户问题：
- 用户可观察到的结果：
- 非目标（本 PR 明确不做）：
- 同主题是否已有未合并 PR：否 / 是（说明为何更新原 PR或为何必须新建）：
- 本 PR 是否是完整垂直切片而非格式化/重复记录/探索性改动：是 / 否（否则不要创建 PR）

## READY 记录

- [ ] 已确认目标、非目标和验收标准
- [ ] 已确认预计修改文件、依赖、风险和回滚方式
- [ ] 已在 INTAKE 检查 Issue/PR，没有重复工作
- [ ] 本 PR 只有一个垂直切片，没有顺手扩展功能
- [ ] 同一切片的代码、测试、用户文案和必要交接文档已尽量合并在本 PR

### 验收标准

- [ ] <可操作、可验证的验收标准>

### 预计修改范围

- 源码：
- 测试：
- 文档/用户文案：

## 实现与文档

- [ ] 代码与测试已同步
- [ ] 用户可见文案已同步
- [ ] 相关需求、路线图、UI 或 ADR 已同步（不相关则说明原因）
- [ ] 编辑器插入类改动已确认使用应用内面板，不依赖浏览器原生 `window.prompt`
- [ ] 没有引入未授权依赖、跨平台范围或无关重构
- [ ] 没有提交密钥、私钥、令牌、签名文件、安装包或本地用户数据

## 验证

测试级别：T0 / T1 / T2 / T3

| 命令        | 结果                            |
| ----------- | ------------------------------- |
| `<command>` | `<pass/fail；失败只写首个根因>` |

- UI 手动路径或截图（如适用）：
- Windows 桌面路径（如适用）：
- CI 记录：`sha=<...> workflow=<...> run_id=<...> conclusion=<...> last_changed_at=<UTC>`

## 发布影响

- 版本分类：无 Release / minor / patch
- 目标版本（如有）：
- 判定理由：
- 本 PR 是否生成安装包：否 / 是
- 是否需要更新 `latest.json`、签名文件和 Cloudflare 镜像：否 / 是
- 若本次不发布，计划在哪个稳定批次发布：

## 风险、回滚与交接

- 已知限制/风险：
- 回滚方式：
- 失败与重试次数：
- 是否发生返工：否 / 是（原因）：
- 合并后下一位 AI 的唯一下一步：

## AI 交接检查

- [ ] `docs/AI-HANDOFF.md` 已更新为短交接
- [ ] 交接包含分支、SHA、完成内容、验证、限制、回滚和唯一下一步
- [ ] 本切片完成后将停止，不自动开启下一批
