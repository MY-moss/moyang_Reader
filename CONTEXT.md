# Moyang Reader 领域术语

这份文件只定义项目中的领域语言，避免产品、代码和 Issue 使用同一个词表达不同概念。

## 内容与空间

**工作区（Workspace）**：用户添加的一个本地文件夹及其可读文档集合。它是浏览、搜索、标签和关系索引的边界。
_避免_: 项目、仓库、云盘

**文档（Document）**：一个可被 Moyang Reader 打开、阅读或编辑的本地文件。
_避免_: 页面、笔记（除非明确指 Markdown 文档）

**阅读库（Library）**：面向用户的工作区名称，强调“添加整个文件夹进行阅读”。代码和技术文档中统一使用“工作区”。
_避免_: 数据库、知识库（除非描述知识关联功能）

**上下文面板（Context Panel）**：围绕当前文档提供目录、出链、反向链接、属性和关系图的右侧信息区域。
_避免_: 右栏、详情栏（可作为 UI 口语，不作为领域名）

## 编辑与关联

**源码模式（Source Mode）**：直接编辑 Markdown 或纯文本字符的模式。
_避免_: 代码模式（Markdown 不等于程序代码）

**所见即所得模式（WYSIWYG Mode）**：以接近最终阅读效果的方式编辑 Markdown，同时仍将 Markdown 文件作为保存结果。
_避免_: 富文本真源、网页编辑模式

**Markdown 真源（Markdown Source of Truth）**：本地 Markdown 文本是文档唯一持久化事实；编辑器内部结构不能取代它。
_避免_: 编辑器 JSON、内部文档树（它们只是临时表示）

**双链（Wiki Link）**：使用 `[[目标]]` 或 `[[目标|别名]]` 指向工作区文档的链接。
_避免_: 普通链接（普通链接专指 Markdown/HTTP 等标准链接）

**嵌入（Embed）**：使用 `![[资源]]` 将工作区中的文档、图片或附件以内嵌形式引用。
_避免_: 附件链接（附件链接不一定内嵌）

**块引用（Block Reference）**：通过显式块 ID 指向文档中的一个内容块，例如 `[[Note#^block-id]]`。
_避免_: 自动段落 ID（项目不会为所有段落自动写入 ID）

**属性（Property）**：Markdown frontmatter 中描述文档的键值信息，例如标题、标签、日期或自定义字段。
_避免_: 数据库字段、配置项（除非讨论程序配置）

## 发布与交接

**功能切片（Feature Slice）**：能够独立运行、测试、审查并由下一位 AI 接手的一组小范围变更。
_避免_: 随手改动、半成品提交

**稳定批次（Stable Batch）**：由多个已合并功能切片组成、经过完整门禁并可以生成公开安装包的版本单元。
_避免_: 每次提交、开发快照

**交接包（Handoff Pack）**：描述任务目标、现状、改动、验证、风险和下一步的最小上下文集合。
_避免_: 全量聊天记录、长日志

## AI 治理与扩展

**治理策略（Governance Policy）**：定义 AI 自动权限、风险边界和必须人工确认事项的稳定规则。
_避免_: 当前任务说明、临时提示词

**执行计划（Execution Plan）**：用户批准的有序功能切片及其依赖、边界和验收集合。
_避免_: 路线图、愿望清单

**运行状态（Runtime State）**：当前功能切片在接手、实施、验证、交付或阻塞阶段的唯一动态事实。
_避免_: 历史交接、发布状态

**批准队列（Approved Queue）**：执行计划中允许 AI 按顺序推进的功能切片序列。
_避免_: AI 自选任务、开放 Issue 全集

**人工确认点（Approval Checkpoint）**：AI 必须停止并由授权维护者确认后才能继续的风险边界。
_避免_: 普通测试失败、可自动恢复的网络错误

**能力接口（Capability Port）**：核心向内部文档、索引、命令或 AI 实现提供的受限能力边界，不等同于公开插件 SDK。
_避免_: 任意脚本插件、稳定第三方 ABI
