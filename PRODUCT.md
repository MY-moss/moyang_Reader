# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

主要用户是重度本地阅读者：他们在 Windows 桌面环境中长期阅读、检索、批注和整理本地长文档，希望文件仍可被其他工具直接读取，并能在没有网络或账号时完整工作。

## Product Purpose

Moyang Reader 是一个 Windows x64、本地优先的长文阅读与轻量编辑工作台。它让用户以普通文件为真源，完成打开阅读库、快速定位、沉浸阅读、搜索、批注、编辑、安全保存、恢复和导出。成功意味着核心任务清晰、快速、可恢复，且不会以私有数据库、云端账号或不透明自动化换取便利。

## Positioning

Moyang Reader 把“普通本地文件始终是真源”与完整的桌面阅读会话结合：阅读位置、工作区索引和应用状态都只是可恢复的辅助层，不能取代用户文件，也不能绕过安全保存边界。

## Operating Context

- 正式平台为 Windows x64；React 浏览器构建只用于开发预览和自动化测试。
- 典型任务是持续阅读 Markdown、TXT、DOCX、PDF 和图片资料，在阅读、源码编辑与所见即所得编辑之间切换。
- 用户可能同时维护多个阅读库，并依赖快速打开、文内查找、工作区搜索、书签、批注、历史和草稿恢复。
- 应用无需登录；核心能力默认离线。更新和用户明确允许的远程资源是独立、可关闭的联网行为。

## Capabilities and Constraints

- Tauri 2 + Rust + React + TypeScript；正式交付限定 Windows x64。
- 保留三栏信息架构、现有快捷键、command ID、Tauri command 名、IPC wire shape、文档格式和设置存储 key。
- 文件系统授权、原子写入、外部修改冲突、草稿和恢复语义不得因 UI 或架构调整而弱化。
- 中文优先，中文与英文界面必须保持完整 key 对齐；迁移范围不保留长期硬编码用户文案。
- 新能力从真实用户动作和现有边界提炼；v1.0 不提前建设空 Provider、插件市场、RAG、万能权限框架或第三方 ABI。
- v1.0 后的候选顺序是 Reader+、Metadata/Knowledge、AI、Interop/Extensions，并继续受真实用户路径和阶段 Gate 约束。

## Brand Commitments

- 产品名称继续使用 Moyang Reader。
- v1.0 采用已经确认的“公共图书馆导视”方向：清晰、有秩序、具有路径与位置感的应用壳层；正文画布保持中性、沉浸、可调。
- 新标志使用“Moyang + 导视/路径/文档定位”语言，避免书页、叶芽和常见 AI 渐变图标套路。

## Evidence on Hand

- 仓库内已有真实可运行的 Windows 应用、发布与恢复流程、约 500 项单测、106 项浏览器场景和 18 项桌面 smoke。
- 当前界面、测试夹具和视觉基线是功能与内容证据，但旧“米色、衬线、墨绿”视觉不是 v1.0 的设计权威。
- 没有可用于产品宣传的客户证明、使用规模、商业指标或第三方背书；后续设计不得虚构。

## Product Principles

1. 用户文件优先：普通文件是真源，索引、缓存和状态都可重建。
2. 阅读优先：长文阅读的清晰度、连续性和恢复能力高于装饰和功能密度。
3. 安全可恢复：任何写入、切换、冲突和失败都必须保留明确恢复路径。
4. 渐进扩展：先交付真实用户动作，再从第二个真实调用关系提炼长期接口。
5. 如实发布：CI 证据与证书、凭据、真机升级等外部条件严格区分。

## Accessibility & Inclusion

v1.0 必须满足 WCAG AA，并覆盖键盘导航、焦点归还、forced-colors、reduced-motion、浅色/深色/高对比主题、中文与英文，以及 Windows 100%/125%/150%/200% DPI。720×600 是最小桌面窗口验收尺寸。
